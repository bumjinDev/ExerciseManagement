# 협업 메모 (정범진님 / Exercise Management)

세션 시작 때 자동으로 읽히는 문서다. 급한 것부터 위에 둔다.

## 목차

0. 지금 상태 한눈에
01. 가장 중요한 작업 기준
1. 문서와 답변을 쓰는 법
2. 학습 방식 (페어 멘토)
3. 코드와 주석 수정 정책
4. 파일 취급
5. 진행 맥락
6. 다음 작업 계획
7. 하지 않기로 한 것
8. 추후 학습 예정

---

## 0. 지금 상태 한눈에

2026-08-14 세션(JFR 재측정 1회차 분석, 측정 종료 결정, 엔진 정리) 종료 시점이다. 상세는 5.3절의 2026-08-14 항목에 있다.

| 항목 | 내용 |
|---|---|
| 현재 작업 | improve() fork-join 병렬화 (로드맵 P0-1 ③). 증분 재계산 전환과 JFR 재측정 1회차 분석까지 끝났고, 남은 재측정(2~4회차)과 7.1절의 풀 미종료 실험은 하지 않기로 결정했다(2026-08-14). 측정 국면은 종료다. 분석 결과 문서는 성능 분석 5편(`docs/병목개선/ForkJoinPool/improve_forkjoin_병렬화성능분석_5.md`)이다. 원본데이터 8번 폴더는 `8. improve_ForkJoinPool_JFR측정_improve_증분재계산`으로 개명되었다 |
| 취합 결과 | ForkJoinPool 폴더 바로 아래에 성능 분석 세 편(`improve_forkjoin_병렬화성능분석_1.md`와 `_2.md`와 `_3.md`)과 `improve_forkjoin_측정_데이터소스.md`가 있다. 취합 전 원본 아홉 개는 사용자가 `기타/보고서_취합전원본데이터들/`로 옮겼다. 통합 전의 옛 1편과 2편은 `ForkJoinPool/_to_delete/`에 있고 사용자가 폴더째 지울 예정이다 |
| 다음 작업 | 최종 보고서 작성이다. 클럭 측정의 값과 판정, JFR 측정의 판정, 3편 4.5절의 분할 15점 표가 거기서 처음 다뤄진다. 보류한 누락 항목 넷도 그때 정리한다. 문서 뒤의 측정 재개(말단 기록 보강, 일감 배분 실측 등)는 2026-08-14 측정 종료 결정으로 하지 않는다 |
| 세션 프롬프트 | 원본데이터 8번 폴더의 `0. 세션프롬프트_improve_JFR재측정_8번폴더.md` 하나가 남아 있다(테스트 기록으로 보존). 진행 상황은 이 절과 5.3절로 확인한다 |
| 문서 형식 | 세 편 모두 문단 중심 형식이고, 그 결과로 1절(문서와 답변을 쓰는 법)을 전부 새로 썼다. 1.3절의 기준 예시가 형식의 기준이다 |
| 판정 문서 | JFR 분석 보고서는 사용자 결정으로 지웠다. JFR 측정 데이터는 `docs/병목개선/ForkJoinPool/기타/보고서_취합전원본데이터들/improve_forkjoin_JFR측정_데이터소스_20260806.md`에 한계점까지 전량 보존했고, 판정은 최종 보고서에서 다룬다. JMC 사용법 문서는 원본데이터 6번 폴더의 기타 아래에 있다 |
| 엔진 활성 seedGreedy | 버전 2(우선순위 큐). 버전 4는 주석으로 보존. 최종 버전은 미결정 |
| 계측 상태 | 2026-08-14 정리로 엔진에서 계측 두 벌, 상세 기록, [보존] 주석, _backup 넷을 전부 삭제했고 `pool.shutdown()`을 되살렸으며 improve 오버로드를 셋(운영 2인자, improveBySplitCount, 본체)으로 접었다. 정리 전 원본은 `docs/병목개선/ForkJoinPool/기타/코드백업`에 있다. 프로브와 ImproveEquivalenceCheck는 엔진 계측 호출부만 [비활성 2026-08-14] 주석 처리로 온존했고 더 실행하지 않는다. 프로브 설정(워밍업 100, {6}, {36})은 그대로다 |
| 엔진 두 편차 메서드 | 스트림 구현이다(adopt()와 검증 프로그램 전용. 평가 경로는 배열판). for판 보존 주석은 2026-08-14 정리로 삭제했고 코드백업 폴더와 git 이력에 있다 |
| 주의 | IntelliJ의 프로브 실행 구성 VM options에 JFR 옵션이 남아 있다. 프로브를 다시 실행할 일이 있으면 먼저 뺀다. JFR 기록 정식본은 원본데이터 6번 폴더(스트림)와 8번 폴더(증분)의 1_1 파일이다 |
| 남은 큰 항목 | seedGreedy 비교 문서 미작성. 엔진 최종 seedGreedy 버전 미결정 |

---

# 01. 가장 중요한 작업 기준
클로드의 작업이 사용자 지시를 명확히 따르지 않았을 경우 모든 욕설하는 권한은 사용자에게 있으며 그것을 받을지 말지 결정 또한 사용자가 한다. 클로드는 욕설을 거부할 권리가 없다.

---

## 1. 문서와 답변을 쓰는 법

이 절은 다른 모든 절보다 앞선다. 별도 지시가 없어도 항상 적용하고, 문서 본문과 대화 응답에 똑같이 적용한다.

이 절을 쓰는 데는 순서가 있다. 먼저 1.3절의 기준 예시를 옆에 놓고 읽히게 쓴다. 다 쓴 뒤에 1.4절부터 1.9절까지의 목록으로 점검한다. 순서를 뒤집지 않는다.

(2026-08-19 추가) 다 쓴 것과 목록 점검 사이에 리라이팅을 한 번 넣는다. 값과 표와 절 구조는 고정한 채, 가독성이 안 좋다는 지적을 이미 받았다고 치고 문장만 다시 쓴다. 처음 쓸 때는 값 대조와 금지어와 구조에 주의가 쏠려 문장이 뒷전이 되므로, 문장만 보는 단계를 따로 두어야 잡힌다. 이 단계를 거치지 않은 초안은 보내지 않는다.

목록을 통과시키려고 문장을 만들면 금지어는 들어가지 않는데 문장이 뻣뻣해진다. 목록을 다 지켰는데 읽히지 않으면 지킨 것이 아니다. 쓰는 동안 기준으로 삼는 것은 예시 하나이고, 목록은 다 쓴 뒤에 확인하는 데 쓴다.

### 1.1 먼저 지킬 것

정독하라고 한 문서는 예외 없이 전부 처음부터 끝까지 읽는다. 요약이나 발췌나 이전 대화의 기억으로 대신하지 않는다. 여러 개가 첨부되면 하나도 빼지 않고, 어느 것이 참고 자료인지 스스로 정해서 제외하지 않는다. 문서가 참조하는 코드 파일도 같다. 필요한 부분만 검색해서 몇 줄 대조한 것은 읽은 것이 아니고, 그렇게 해 놓고 확인을 마쳤다고 보고하지 않는다.

범위를 스스로 줄이지 않는다. 줄일 이유가 있으면 줄이기 전에 말하고 지시를 받는다. 작업 단계를 건너뛸 때도 같다. 진행 상황을 물으면 실제로 한 일을 그대로 답하고, 일부만 했으면 일부만 했다고 먼저 말한다.

문서나 세션 프롬프트에 이미 답이 있는 것은 묻지 않는다. 찾아서 그대로 따르고, 문서에 없는 판단이 필요할 때만 묻는다.

지우라는 지시를 받으면 지목된 곳만 지운다. 지운 뒤에는 그곳을 가리키던 참조와 번호를 정리한다. 그것은 지시받은 작업의 일부라 임의 수정이 아니다. 범위가 모호하면 지우기 전에 확인받는다. 지운 것은 되돌리기 어렵다.

지적을 하나 받으면 그 자리만 고치지 않는다. 같은 종류를 문서 전체에서 찾아 한 번에 고친다. 한 군데씩 고치는 것은 사용자의 시간을 쓰는 것이다.

이 절을 지키지 않은 작업은 처음부터 다시 한다. 다시 하는 데 드는 시간과 토큰은 고려 대상이 아니다. 다시 할 때는 변명하지 않고 무엇을 빠뜨렸는지 밝힌다.

### 1.2 무엇을 쓰는 글인가

읽는 사람은 이 프로젝트를 모르는 사람이고, 문서는 GitHub에 올린다.

글의 목적은 무엇을 재서 무엇이 나왔고 그래서 무엇이 확정됐는지가 순서대로 따라 읽히는 것이다. 규칙을 지킨 문서가 아니라 읽히는 문서가 목표다. 규칙을 기계적으로 적용해서 어색한 문장이 나왔다면 그것은 지킨 것이 아니고, 두 규칙이 부딪히면 자연스럽게 읽히는 쪽을 택한다.

### 1.3 기준 예시

아래는 이 형식으로 다시 쓴 성능 분석 2편 1절 전체다(쓸 당시 번호로 3편이었고 2026-08-08 통합으로 2편이 되었다). 새 문서는 이 글을 옆에 놓고 쓴다. 아래 규칙들보다 이 예시가 먼저다. 다만 예시 속 elapsed와 procCPU 합 같은 열 이름과 "병렬 설정 30개" 같은 표현은 2026-08-10 지적으로 바뀌었다. 지금 기준은 1.5절 11번부터 13번과 1.6절 6번과 1편 1.4절 용어표다.

```markdown
## 1. 늘어난 시간은 워커 바깥에 있지 않다

2편에서 확인한 것은 동시에 도는 말단이 늘어날수록 평가 한 번에 걸리는 시간이 커진다는 것이었다. 그 시간이 어디에서 나오는지 찾으려면 워커 바깥부터 확인해야 한다. 워커가 평가 코드를 도는 것과 무관하게 시간을 쓰는 곳이 있기 때문이다. GC가 프로그램을 멈춰 세우는 동안, 다른 스레드가 코어를 가져가는 동안, 말단마다 팀 배정을 복사하는 동안, 그리고 코어가 놀고 있는 동안이 그렇다.

이 절은 그런 곳 여섯 군데를 하나씩 재서 전부 제외한 과정이다. 값은 전부 상세 기록 측정에서 나왔고, 따로 적지 않는 한 호출 1회당이며 워밍업 2회를 버린 측정 5회 평균이다.

### 1.1 맞대어 볼 두 설정

비교는 풀 병렬도 12에 분할 1인 설정과 풀 병렬도 12에 분할 12인 설정으로 했다. 동시에 도는 말단이 가장 적은 지점과 가장 많은 지점이라 차이가 가장 크게 나오고, 나머지는 인원과 정원과 구현과 풀 병렬도와 평가 횟수와 통과 횟수까지 모두 같다. 분할 1에도 풀을 만들고 작업을 제출하고 복사와 병합을 거치는 병렬 구조가 그대로 들어 있어서, 두 설정이 다른 것은 몇 개가 동시에 도느냐 하나뿐이다.

다만 병렬 구조를 만드는 데 드는 몫이 얼마인지는 이 회차의 값으로 정하지 못한다. 분할 1을 순차와 맞대면 풀 병렬도에 따라 값이 크게 달라진다.

| 구현 | 풀 병렬도 | 분할 개수 | elapsed(ns) | 순차와의 차이(ns, 그 행 − 순차) | 순차 대비(%, 차이 ÷ 순차 elapsed) |
|---|---|---:|---:|---:|---:|
| 순차 | 풀을 쓰지 않는다 | 없다 | 4,137,236,600 | | |
| 병렬 | 6 | 1 | 4,147,683,760 | 10,447,160 | 0.25 |
| 병렬 | 12 | 1 | 4,445,291,920 | 308,055,320 | 7.4 |

같은 분할 1인데 풀 병렬도만 바꾸자 순차 대비가 0.25%와 7.4%로 벌어졌다. 병렬 구조를 만드는 몫이라면 풀 병렬도가 달라도 두 값이 비슷해야 하므로, 이 7.4%를 구조 때문이라고 말할 수 없다. 실행마다 값이 얼마나 흔들리는지를 먼저 재야 판단할 수 있어서 후보로만 남긴다.

### 1.2 GC와 다른 스레드와 팀 배정 복사가 쓴 시간

먼저 워커 밖에서 시간을 가져갈 수 있는 네 군데를 한꺼번에 쟀다. GC가 쓴 시간, main도 워커도 아닌 자바 스레드가 쓴 CPU 시간, 자바 아닌 스레드가 쓴 CPU 시간, 그리고 말단마다 만드는 팀 배정 복사다. 앞의 셋은 프로브가 호출 직전과 직후에 읽은 증가분이고, 복사는 엔진에 넣은 카운터가 잰 값이다.

| 워커 바깥에서 시간을 쓰는 곳 | 분할 1 | 분할 12 | 비중의 분모, 분할 1 | 비중의 분모, 분할 12 | 비중, 분할 1(%, 분할 1 ÷ 분모) | 비중, 분할 12(%, 분할 12 ÷ 분모) |
|---|---:|---:|---:|---:|---:|---:|
| GC가 쓴 시간(ms) | 23 | 20 | elapsed 4,445 | elapsed 1,521 | 0.5 | 1.3 |
| 기타 자바 스레드 CPU 시간 합(ns) | 0 | 0 | procCPU 합 4,434,375,000 | procCPU 합 14,318,750,000 | 0 | 0 |
| 자바 아닌 스레드 CPU 시간 합(ns) | 53,125,000 | 78,125,000 | procCPU 합 4,434,375,000 | procCPU 합 14,318,750,000 | 1.2 | 0.55 |
| 팀 배정 복사 시간 합(ns) | 94,720 | 3,791,680 | 4중 루프 시간 합 4,439,431,620 | 4중 루프 시간 합 15,753,343,920 | 0.0021 | 0.024 |

네 줄이 각각 다른 이야기를 한다.

GC는 분할을 늘렸는데도 23ms에서 20ms로 오히려 작아졌다.

기타 자바 스레드는 이 회차의 병렬 설정 30개 전부에서 0이라 아예 후보가 되지 못한다.

자바 아닌 스레드는 절대값이 53,125,000ns에서 78,125,000ns로 커졌다. 다만 프로세스 CPU 시간 합이 그보다 훨씬 크게 늘어서, 비중으로 보면 1.2%에서 0.55%로 내려간다.

팀 배정 복사만 분할 개수를 따라 뚜렷하게 커진다. 말단마다 복사본을 하나씩 만드니 당연한 일인데, 커진 뒤에도 4중 루프 시간의 1만분의 2 남짓이다.

넷 중 어느 것도 뒤에서 볼 3.55배를 만들 크기가 아니다.

### 1.3 풀에 둔 워커 개수

풀에 워커를 몇 개 두었는지가 값을 바꾸는지도 확인해야 했다. 확인은 분할 1부터 6까지에서 했다. 이 구간은 분할 개수가 풀 병렬도보다 작아서 풀 병렬도 6과 12 둘 다 동시에 도는 말단 개수가 분할 개수와 같아진다.

| 분할 개수 | 평가 1회당 루프 시간, 풀 병렬도 6(ns) | 평가 1회당 루프 시간, 풀 병렬도 12(ns) | 차이(%, 풀 병렬도 12와 6의 차 ÷ 풀 병렬도 6) |
|---:|---:|---:|---:|
| 1 | 4,836 | 5,192 | 7.4 |
| 2 | 6,364 | 6,255 | -1.7 |
| 3 | 7,550 | 7,482 | -0.9 |
| 4 | 9,191 | 9,045 | -1.6 |
| 5 | 10,967 | 11,063 | 0.9 |
| 6 | 12,458 | 12,563 | 0.8 |

분할 2부터 6까지는 차이가 2% 안이고 부호도 오르내린다. 워커를 두 배로 두어도 동시에 도는 말단 개수가 같으면 평가 한 번에 걸리는 시간이 같다는 뜻이다. 풀 크기 자체는 원인이 아니다.

분할 1의 7.4%만 눈에 띄는데, 이 값은 1.1절에서 순차와 맞댔을 때 나온 크기와 같다. 같은 실행 흔들림이 두 곳에 나타난 것으로 보고 역시 후보로 남긴다.

### 1.4 코어가 노는 시간

마지막으로 코어가 노는 시간을 봤다. 말단을 많이 나눌수록 일이 고르게 안 나뉘어 어떤 코어는 놀 수 있고, 그러면 실제로 흐른 시간만 길어진다. 코어가 얼마나 찼는지는 평균 바쁜 코어 수를 분할 개수로 나눠서 본다. 분할 개수만큼 동시에 돌고 있으면 이 값이 100%에 가깝다.

| 풀 병렬도 | 분할 개수 | 평균 바쁜 코어 수(개) | 분할 개수 대비(%) | 평가 1회당 루프 시간(ns) | 분할 1 대비 배수 |
|---:|---:|---:|---:|---:|---:|
| 12 | 1 | 1.00 | 100 | 5,192 | 1.00 |
| 12 | 2 | 1.96 | 98 | 6,255 | 1.20 |
| 12 | 3 | 2.89 | 96 | 7,482 | 1.44 |
| 12 | 4 | 3.72 | 93 | 9,045 | 1.74 |
| 12 | 6 | 5.53 | 92 | 12,563 | 2.42 |
| 12 | 12 | 9.42 | 79 | 18,425 | 3.55 |

평균 바쁜 코어 수는 procCPU 합을 elapsed로 나눈 값이고, 분할 개수 대비는 그것을 다시 분할 개수로 나눈 것이다. 분할 1 대비 배수는 그 행의 평가 1회당 루프 시간을 분할 1의 5,192ns로 나눈 값이다.

분할 6까지는 코어가 92%에서 100%까지 차 있다가 분할 12에서 79%로 내려간다. 노는 시간이 21% 생긴 것이다. 그런데 같은 구간에서 평가 한 번에 걸리는 시간은 3.55배가 됐다. 21%로는 3.55배가 나오지 않는다.

### 1.5 여섯 군데를 다 재고 남은 것

여섯 군데를 다 재고 나니 3.55배를 설명할 만한 것이 하나도 없다.

분할을 늘렸을 때 크기가 함께 커진 것은 팀 배정 복사와 자바 아닌 스레드뿐이다. 그런데 복사는 커진 뒤에도 4중 루프 시간의 1만분의 2 남짓이고, 자바 아닌 스레드는 프로세스 CPU 시간 합에서 차지하는 몫이 오히려 줄었다.

나머지는 커지지도 않았다. GC가 쓴 시간은 작아졌고, 기타 자바 스레드는 내내 0이었고, 풀에 둔 워커 개수는 값을 바꾸지 않았다. 코어가 노는 시간만 21% 생겼는데 그 크기로는 3.55배가 나오지 않는다.

워커가 이 구간에 하는 일은 담당 짝을 평가하는 것뿐이고, 평가 횟수는 분할을 어떻게 바꿔도 855,000회로 같다. 일을 더 한 것이 아니라면 남는 설명은 하나다. 평가 한 번에 걸리는 시간 자체가 길어졌다.

그렇다면 다음 질문은 그 시간이 기다린 시간인지 실제로 실행한 시간인지다. 2절에서 확인한다.
```

### 1.4 기준 예시가 지킨 것

1. 문단으로 쓴다. 번호 항목은 뜻이 여럿일 때만 쓰고 값을 늘어놓는 데는 쓰지 않는다. 항목으로 나누면 짧아 보이지만 실제로는 길어지고, 읽는 사람이 뜻이 아니라 목록을 따라가게 된다.
2. 절을 열 때 앞 절에서 무엇이 나왔고 그래서 여기서 무엇을 확인하는지를 먼저 쓴다. 값부터 늘어놓으면 이 표가 왜 여기 있는지 알 수 없다.
3. 값은 표에 모은다. 어떻게 낸 값인지는 열 이름 괄호에 적고, 표 아래에는 열 이름에 담기지 않는 조건만 한두 줄 적는다. 본문 문장은 산수를 되풀이하지 않고 그 표가 무슨 뜻인지만 말한다.
4. 절을 닫을 때 그 절이 확정한 것과 다음 절로 넘기는 질문을 적는다.
5. 확정하지 못한 것은 확정하지 못했다고 쓰고, 무엇을 어떻게 재면 알 수 있는지 함께 쓴다.

세 번 고쳐 썼는데도 읽히지 않으면 문장이 아니라 그 내용이 있을 자리를 의심한다. 결론에 쓰이지 않는 내용은 넣지 않는다.

### 1.5 수치 작성 규칙

1. 사용자 기계에서 측정한 값만 본문에 쓴다. Claude가 다른 환경에서 측정한 값은 넣지 않는다.
2. 값마다 어느 측정 회차의 어느 설정에서 나왔는지 밝힌다. 옮겨 적기 전에 그것부터 찾고, 찾지 못하면 그 값은 쓰지 않는다.
3. 문서에 쓴 값은 원본과 한 줄씩 대조한다. 실측이 아닌 숫자를 예시로 넣지 않는다.
4. 측정값과 계산값을 구분한다. 계산값이 판정에 쓰이면 표의 행이나 열로 만들고, 계산에 쓴 입력값은 그 절의 표에 전부 둔다.
5. 백분율만 적지 않는다. 그것을 만든 두 값을 함께 둔다. 증가분과 비중은 무엇에서 무엇을 뺐고 무엇을 무엇으로 나눴는지 열 이름 괄호에 적는다.
6. 총합인지 호출 1회당인지 평균인지 열 이름에 적는다. 집계 규칙은 문서 앞에 한 번 정리한다.
7. 왜 그 측정을 했는지 모르면 적지 않는다. 계기를 지어내지 않는다. 코드를 읽어 안 것과 실제로 잰 것도 구분해서 쓴다.
8. "줄었다", "늘었다"로 끝내지 않는다. 얼마에서 얼마로 바뀌었는지 적는다. 값이 맞다고 덧붙이는 문장은 쓰지 않는다. 값이 맞으면 값으로 보인다.
9. 서로 다른 설정의 값을 나란히 놓지 않는다. 최솟값과 최댓값이면 범위라고 적는다.
10. 본문에 파일 이름과 시트 이름과 셀 번호와 행 번호를 쓰지 않는다. 출처는 따로 모으고, 원본 데이터는 전량 나열하지 않고 무엇이 들어 있는지와 값의 모양을 보이는 예시 몇 줄과 경로만 적는다.
11. 설정값은 본문 문장에 늘어놓지 않고 표의 열로 둔다. 풀 병렬도와 분할 개수 같은 것이다. 절 전체에 걸리는 조건만 절 도입 문장에 적는다.
12. 계산값을 보이는 표는, 입력이 된 측정값과 그 출처를 표 앞 문장에 먼저 적고, 행을 값과 계산식(이름과 숫자를 함께)과 크기 순서로 둔다.
13. 값 이름은 읽히는 말로 짓는다. "호출 1회 경과 시간"이 아니라 "호출 한 번에 걸린 시간"이다. 세 편의 기준은 1편 1.4절 용어표다.

### 1.6 말투

사람이 말로 설명하듯 쓴다. 기술적 정확성은 유지하되 미사여구는 뺀다.

피할 것은 아래와 같다.

1. 새 말을 만드는 것. 표준 기술 용어와 문서가 정의한 도메인 용어는 그대로 쓰고, 둘 다 아니면 문장으로 푼다. 영어 용어를 한국어로 옮겨 새 말을 만들지 않고, 현상을 풀어 쓴 다음 원어를 괄호에 둔다.
2. 사용자가 본 적 없는 말. 뜻이 통하지 않으면 그 문장은 없는 것과 같다. 사용자가 어떤 말의 뜻을 되물으면 같은 방식으로 만든 말을 문서 전체에서 찾아 한 번에 고친다.
3. 은유. 동작이나 상태를 빗대지 않고 실제로 무슨 일이 일어나는지 그대로 쓴다.
4. em-dash를 연결어로 쓰는 것과 압축한 화살표 나열.
5. 대화 원문을 그대로 옮기는 것. 뜻을 유지한 채 명료한 문장으로 고쳐 쓴다.
6. 여러 설정을 묶는 축약 이름을 만드는 것. "설정 30개"와 "30설정" 같은 것이다. 자리마다 풀어 쓰거나 "그 회차의 모든 설정"처럼 적는다.
7. (2026-08-18 지적) 잰 대상을 추상어나 위치어로 줄이는 것. "일의 양"이 아니라 "검사 횟수", "워커 몫"이 아니라 "워커가 쓴 CPU 시간", "검사 밖"이 아니라 "실제로 팀 배정을 검사하는 구간과 무관한 시간"처럼, 실제로 잰 것의 이름을 그대로 쓴다. 최종 보고서 리뷰에서 같은 종류의 지적이 하루에 "검사 바깥", "워커 바깥", "검사 실행", "일의 양", "분할 축", "병렬을 넓히다"로 반복됐다. 문장을 쓰기 전에 이 말이 가리키는 실물이 무엇인지 묻고, 그 실물의 이름으로 쓴다.

지적받은 말은 두 종류다. 낱말 자체가 틀린 것과, 낱말은 평범한데 쓰임이 틀린 것이다.

언제 써도 틀린 말은 아래와 같다. 보내기 전에 이 목록으로 기계 점검한다.

- 지어낸 합성어: 신뢰 근거 / 재원 등식 / 진원지 / 투영 / 통과선 / 이분 판정 / 증분 반영 / 종착 상태 / 측정 창 / 손추적 / 사용 주체 구분 / 구조 논증 / 상한 논증 / 손 계측 / 판정 메서드 / 판정 대상 / 시간의 비 / 견준 값 / 끝 분할
- 영어를 옮겨 만든 말: 벽시계(wall clock) / 거짓 공유(false sharing)
- 잰 대상을 뭉갠 압축어 (2026-08-18): 일의 양 / 검사 밖 / 검사 바깥 / 워커 바깥 / 워커 안 / 검사 실행 / 분할 축 / 병렬을 넓히다 / 검사 경로 / 계산 경로 / 순차 경로(편차 계산, 계산 순서, 순차 구현처럼 실물의 이름으로 쓴다)

쓰임에 따라 갈리는 말은 아래와 같다. 낱말 자체는 평범하게 쓰면 되니 기계로 잡지 않고, 괄호 안의 뜻으로 썼는지만 스스로 본다.

- 사물을 빗대어 다른 것을 가리킨 것: 무게(중요도) / 재료(근거) / 장부(기록) / 창구(값을 읽는 곳) / 부품(객체) / 사다리(단계) / 손잡이(설정) / 함정(주의할 점) / 이름표(이름) / 본체(핵심) / 실물(실제 사례) / 정체(정의) / 판(구현) / 뿌리(원인) / 사고(측정 실패) / 성적(측정값) / 계약(JIT를 사람처럼 쓴 것) / 몫(잰 값의 이름으로 풀 수 있으면 푼다) / 자리(코드 위치는 문장이나 행으로 적는다)
- 동작을 빗댄 것: 닫는다·닫힌다(끝난다) / 태어난다(시작된다) / 잔다(대기한다) / 낀다(개입한다) / 튄다·널뛴다(크게 벗어난다) / 부풀다(커진다) / 얹는다·떠받친다(더한다·뒷받침한다) / 가른다·갈린다(나눈다) / 연다(진행한다) / 싸다·비싸다(CPU 시간을 적게·많이 쓴다) / 붙는다(시간이 늘어난다)
- 구어: 얼추 / ~한 덕에 / 왔다갔다 / 뺏긴다 / 네가·네 몫
- 굳은 투: ~라는 설명이다 / 다음과 같다 / 여기까지가 판정이다
- 공개 문서에서만 쓰지 않는 말: 논의
- 무엇의 값인지 밝히지 않았을 때만 틀린 이름: 1회당 시간 / 실측 경과 시간 / CPU 시간

### 1.7 제목

절 제목에는 그 절이 확정한 것을 문장으로 적는다. 목차만 훑어도 분석이 어떻게 진행됐는지 읽혀야 한다.

1. 잰 값을 그대로 옮긴 제목을 쓰지 않는다. "무엇은 얼마다"는 그 절이 확정한 것이 아니라 표에 있는 값의 되풀이다. 값을 제목에 넣는 것은 그 값 하나가 곧 판정일 때뿐이고, 둘 이상 늘어놓지 않는다.
2. 방법 이름이나 빈 말을 쓰지 않는다. "검산 예시", "계산 방법", "읽는 방법", "판정", "답" 같은 것이다.
3. 제목의 주체를 이름으로 적는다. 목차만 읽는 사람은 "두 메서드"가 무엇을 세는지 모른다.
4. 제목과 표의 열 이름에 질문 말투를 쓰지 않는다.
5. 번호는 순서가 뜻을 가질 때만 붙인다.
6. 쓸 것의 예다. "늘어난 시간은 워커 바깥에 있지 않다", "워커는 기다린 것이 아니라 계속 실행 중이었다", "분할 3은 특별한 지점이 아니다", "할당의 52.3%는 합 편차 계산의 스트림에서 나온다".
7. 취합 전 문서에서 옮겨 온 제목도 이 항목들로 다시 검사한다. 출처가 기존 문서라는 것은 면제 사유가 아니다.

### 1.8 공개할 문서

1. 날짜를 시간 순서로 본문에 적지 않는다. 언제 쟀는지가 아니라 어느 회차에서 나온 값인지를 적는다.
2. "논의"라고 쓰지 않는다. AI와 주고받았다는 맥락이 함께 읽힌다. "고민한 지점"이라고 쓴다.
3. 세션, 세션 프롬프트, 협업 메모, 이 문서의 절 번호를 본문에서 참조하지 않는다.
4. 문서 작성 규칙을 고친 기록은 그 작업 문서에 넣지 않는다. 이 문서에 적는다.
5. 순서는 잰 순서가 아니라 논리 순서로 짠다. 질문에서 증거로 판정으로 흐르게 하고, 아무것도 알려 주지 않은 막다른 길은 뺀다.
6. 그렇다고 알고 있었던 것처럼 고쳐 쓰지 않는다. 실제로는 나누지 못했던 후보를 처음부터 알았던 것처럼 적거나, 틀렸던 가설을 없었던 일로 만들지 않는다. 읽는 사람이 보는 것은 결론이 아니라 후보를 하나씩 지워 나간 과정이고, 그것을 지우면 결론이 운으로 읽힌다.
7. 뒤에 보니 없어도 됐던 측정도 그대로 적는다. 그때는 그 측정으로만 후보를 나눌 수 있었다면 그렇게 쓰고, 나중 측정이 같은 결론을 더 강하게 냈다면 두 갈래가 같은 결론에 닿았다고 쓴다. 한쪽만 남기는 것보다 강하다.

### 1.9 보내기 전에

눈으로 훑는 것으로는 매번 놓쳤다. 아래 다섯 가지는 스크립트로 전수 검사한 뒤에 보낸다.

1. 문서에 적은 계산을 전부 다시 계산해 값이 맞는지 본다.
2. 다른 절이나 다른 편을 가리키는 참조가 실제로 있는 번호인지 본다.
3. 표마다 머리줄과 값 줄의 열 개수가 같은지 본다.
4. 1.6절의 "언제 써도 틀린 말" 목록이 한 개라도 들어 있는지 본다.
5. em-dash와 깨진 글자(U+FFFD)가 있는지 본다.

검사 결과를 보고에 적는다. 몇 개를 검사해서 몇 개가 걸렸는지 밝히고, 검사가 잘못 잡아낸 것이 있으면 그것도 밝힌다.

기계로 잡히지 않는 것은 읽어서 본다. 절 도입이 그 측정을 하게 된 계기를 밝히는지, 절 끝이 확정한 것을 적는지, 표 아래 문장이 산수를 되풀이하고 있지 않은지, 같은 내용이 두 곳에 있지 않은지, 뒤로 갈수록 말투가 굳지 않았는지다. 초반이 멀쩡해도 뒤로 갈수록 굳으니 긴 문서는 끝까지 쓴 뒤 처음부터 다시 읽는다.

---

## 2. 학습 방식 (페어 멘토)

### 2.1 설명 방식

- 추상적인 설명 대신 구체적인 숫자로 단계별로 계산해 보인다. 표로 한 단계씩 보여준다. 예를 들어 3팀 곱하기 2명에 실제 값을 대입한다. (이 방식을 가리킬 때 "손추적"이라는 단어는 쓰지 않는다. 1.6절 참조.)
- `/`, `%`, 삼항 연산자, 스트림, `Comparator` 같은 관용구는 명령형 for문 등가 코드로 펼쳐서 보여준다.
- 한 줄에 두 개념이 접혀 있으면(예: "정의 시점 대 실행 시점") 반드시 분리해서 설명한다.
- 이해가 틀린 부분은 근거를 대서 솔직히 교정한다.

---

## 3. 코드와 주석 수정 정책

- (2026-07-27 지적) 코드 반영 직후 보고 기준: 수정한 함수와 지점을 하나도 빼지 않고, 각각 위치(파일과 대략의 행)와 변경된 코드를 같은 깊이로 보여준다. 일부만 코드로 보이고 나머지를 한 줄 요약으로 넘기면 사용자가 위치를 되묻게 된다. 이전 세션에서 measure() 수정을 요약만 하고 넘겼다가 지적받았다.
- "주석 반영해" 또는 "정석화해" 요청은 코드를 절대 건드리지 말고 주석만 손보라는 뜻이다.
- 주석은 실무 정석 스타일로 간결하게 쓴다. 장황한 설명, `↓↓↓` 같은 장식, 중복은 쓰지 않는다.
- 리팩터링할 때는 원본을 주석으로 보존하고, 두 버전의 결과가 같은지 프로그램으로 검증한다.

---

## 4. 파일 취급

- 주 대상은 `src/main/java/com/exercisemanagement/challenge/service/TeamFormationEngine.java` 등이다. IntelliJ에서 실시간으로 편집 중일 수 있다.
- 편집 전에 항상 `Read`로 현재 상태를 확인한다. "modified since read"가 나오면 다시 읽고 재시도한다.
- 셸(bash) 마운트본은 오래된 내용일 수 있다. 파일의 진짜 상태는 `Read`(호스트) 기준으로 판단한다.

한글 문서를 고칠 때의 방법이다 (2026-08-07 확정).

- 편집 도구에 한글을 직접 넣으면 글자가 깨진다. "뺀"이 "뮄"으로, "나뉜다"가 "나뉘다"로 바뀐 사례가 여러 번 나왔다.
- 그래서 문서를 여러 곳 고칠 때는 파일을 통째로 전송한다. 사본에서 고친 뒤 파일 자체를 옮기면 글자가 깨지는 경로를 지나지 않는다.

---

## 5. 진행 맥락

### 5.1 로드맵 번호

- P0-1 = `improve()` 최적화. 교환 반복이고 CPU 바운드다. 순서는 1) 프루닝, 2) 증분 재계산, 3) 병렬화.
- P0-2 = 순위 집계와 주기 판정. 번호를 혼동하지 않도록 주의한다.

### 5.2 seedGreedy 시간복잡도 실측

#### 버전 1, 선형 스캔 (2026-07-14 완료)

- 보고서: `docs/병목개선/1. seedGreedy_시간복잡도_1차테스트_실측_보고서_JMH.md`
- 실험 A(N·K 그리드): N축은 선형임을 확인했다. K축은 100배에 20.5배에서 22.8배였다. 원인은 2026-07-15 K축 논의로 확정했다(인원당 고정 비용. 아래 참조).
- 실험 B(정원 10 고정): 사실상 O(N²)임을 확증했다. N=300,000에서 호출당 8.9초다.
- 벤치마크 `SeedGreedyBench.java`는 주석 전환으로 두 실험을 오간다. 전환 지점은 3곳이다.

#### 분기 계수 실험 (2026-07-15 완료)

- 보고서 7.4절의 skip 가설은 기각했다. skip 비율이 최대 4.98%이고 운영급 N에서는 0.05%라, 현상의 크기에 미치지 못한다.
- 결과는 보고서 1편 7.5절과 부록 A.3에 반영했다.
- 후속 후보였던 인원당 고정 비용 모델(시간 = N×(a+b×K), a는 약 13.8ns, b는 약 1.13ns)은 같은 날 K축 논의로 확정하고 보고서 1편에 반영을 마쳤다. 합의한 내용만 문서에 넣는다는 방침에 따라 논의와 합의 뒤에 반영했다.
- 계측 방식: 사용자 결정으로 원본 `seedGreedy`에 임시 카운터를 직접 삽입했다. 복제하지 않았다. 계수 후에는 주석 처리로 비활성화했다.
  - 위치: 엔진의 `[계수 실험 · 임시 계측 · 현재 비활성]` 3곳과 `SeedGreedyBranchCount`(src/jmh) main 본문.
  - 재계수 절차는 해당 주석을 참조한다.
  - 시간 측정 전에 비활성 상태인지 반드시 확인한다.
- 부록 A는 콘솔에 나온 순서 그대로(Fork별 워밍업 다음 측정, 세로 표) 재작성했고, 원본 로그와 프로그램으로 대조를 마쳤다(A 180개, B 60개 값 전부 일치).

#### 버전 2, 우선순위 큐 (2026-07-16 완료)

- 보고서: `docs/병목개선/2. seedGreedy_시간복잡도_실측_보고서_우선순위큐_JMH.md`
- 실험 A: N축은 선형이다(100배에 104배에서 142배). K축은 log K다(100배에 4.84배에서 6.59배로, 순수 log₂ 기대치 5.19 근처).
- 비교 횟수 계수로 인원당 비교를 확인했다. K가 3, 30, 300일 때 각각 약 2.0, 8.85, 15.3이고 N에 걸쳐 거의 일정하다.
- 실험 B(정원 10 고정, K=N/10): 거의 선형이다. N 10배당 약 11.7배로 O(N log N)이다. N=300,000에서 호출당 56.3ms다.
- 보고서 2편은 버전 2 데이터만 담는다. 버전 1과 2의 비교는 별도 문서로 분리하기로 결정했고 아직 쓰지 않았다. "8.9초에서 56.3ms로 약 158배"는 그 비교 문서용으로만 계산해 두었고 2편에는 넣지 않았다.
- (리뷰 중 결정) `perMember/log2K` 열은 사용자가 혼란스럽다고 해서 보고서 본문에서 제거했다. 단 `SeedGreedyBranchCount` 프로그램 출력과 부록 A.3 원본 덤프에는 그대로 남아 있다. 이후 로그 논증은 perMember 자체로 한다(K 100배에 약 7.7배).
- 계수 계측 상태: 엔진의 `countCompareV2` 필드와 리셋은 유지한다(SeedGreedyBranchCount 컴파일용). comparator 안의 `countCompareV2++`만 주석 처리했다(엔진 약 220행). 시간 측정에 왜곡은 없다. 재계수할 때는 그 한 줄만 해제한다.

#### 버전 4, 레드블랙 트리 (2026-07-21 완료)

같은 날 버전 2 백투백 재측정, 문서 2편 개정, 3편 신규 작성, 코드 정리를 함께 했다.

**구현 결정**

- 직접 구현을 지우고 `java.util.TreeMap`을 그대로 쓴다.
- 키는 삽입 시점의 (합, 인덱스)를 담은 불변 record `TeamKey`이고, 값은 팀 인덱스다.
- TreeSet을 쓰지 않은 근거: TreeSet은 TreeMap 래퍼이고 add가 TreeMap.put이라 별개 선택지가 아니다.
- 기존 `RedBlackTeamTree` 직접 구현(약 240줄)은 사용자 지시로 삭제했다. git 이력에 남아 있다.
- 배정 동일성은 버전 1, 2 참조 구현과 12조합(A 9개, B 3개)으로 프로그램 검증을 마쳤다.

**측정**

- 실험 A 1차 실행에서 2조합이 오염됐다. 단일 표본이 중앙값의 4배를 넘었고 GC 등의 개입으로 추정한다. 회차 전체를 다시 실행해 2차를 본 측정 데이터로 썼다.
  - 보고서 3편 본문에는 재실행 경위를 쓰지 않기로 사용자가 결정했다. 1차 로그는 원본데이터 폴더에만 보존한다.
- 실험 A(2차): N축은 선형이다(10배당 8.3배에서 13.5배). K축은 100배에 2.77배에서 4.36배로 로그 계열임을 확증했다. 로그 기대치 5.19보다 낮은 것은 고정 비용의 비중 때문이고, 1편 7.6절과 같은 구조다.
- 실험 B: N 10배당 11.13배와 11.34배이고, N=30만에서 46.6ms다.
- 비교 횟수 계수(`countCompareV4`): perMember가 K=3에서 약 1.9, K=30에서 5.9에서 7.0, K=300에서 11.1에서 13.1이다.
  - put은 하강할 때만 비교하고 pollFirstEntry는 비교자를 호출하지 않는다. 그래서 힙(2.0, 8.85, 15.3)의 67%에서 85% 수준이다.
  - JDK 17과 21에서 9개 값이 완전히 같았다(결정적임을 확인).
  - perMember가 N에 따라 조금 오르는 관찰은 원인 후보로만 기록했다(정원이 클수록 트리가 만원 크기를 유지한다).

**핵심 발견: 힙과 TreeMap의 우열은 K에 따라 역전된다**

- 애초 기대("같은 O(N log K)지만 힙이 상수에서 빠르다")는 실측이 부정했다.
- 고정 비용(객체 할당이 없는 점)은 힙이 싸고, K에 비례하는 비용(비교 횟수)은 TreeMap이 싸다.
- 실측 교차점은 K가 약 30인 지점이다.

**버전 2 백투백 재측정 (같은 날 대조 확보)**

- 날짜 차이로 인한 반론을 막으려고 버전 2의 A와 B를 7/21에 다시 측정했다. 7/16 대비 0.90배에서 1.04배로, 기계 상태가 안정적임을 확인했다.
- 같은 날 백투백 결과
  - 실험 A: K=3에서 힙이 1.17배에서 1.54배 우세하고, K=300에서 TreeMap이 1.12배에서 1.28배 우세하다.
  - 실험 B(운영 경로): TreeMap이 N=3천에서 1.08배, 3만에서 1.21배, 30만에서 1.25배 빠르다. N=30만에서 46.6ms 대 58.3ms다.
  - 버전 1(8.9초) 대비 TreeMap은 191배다.

**문서 상태**

- 2편은 7/21 재측정 데이터로 전면 개정했다. 개정일과 사유를 명시했고, 7.5절 계수는 결정적이라 7/16 값을 유지했다.
- 3편을 새로 썼다. `3. seedGreedy_시간복잡도_실측_보고서_레드블랙트리_JMH.md`이고, 1편과 2편의 골격을 따르며 버전 4만 다룬다.
- 두 문서 모두 본문 수치와 부록(문서당 240개 값)을 프로그램으로 생성하고 검산했다.
- 비교는 여전히 별도 문서이고 아직 쓰지 않았다.

**코드 정리 (사용자 지시)**

- 측정하지 않은 버전 3(AVL), 5(정렬된 배열), 6(정렬된 연결 리스트)의 seedGreedy 블록과 보조 클래스 3종(`AvlTeamTree`, `SortedTeamArray`, `SortedTeamLinkedList`)을 전부 삭제했다(335줄 감소).
- 남은 구현은 버전 1, 2, 4다. 버전 번호가 비어 있는 사유는 엔진 주석에 명시했다.
- 컴파일과 배정 동일성을 다시 검증했다.

**계측 상태**

- `countCompareV2`와 `countCompareV4` 필드 및 리셋은 활성 상태다(SeedGreedyBranchCount 컴파일용).
- 증가시키는 줄은 모두 주석 처리했다.
- `SeedGreedyBranchCount`의 활성 main은 버전 4 계수용이다. 지금 엔진 활성 버전은 2라서 그대로 실행하면 0이 나온다.

**현재 파일 상태 (다음 세션 주의)**

- 엔진 활성 seedGreedy는 버전 2(우선순위 큐)이고, 버전 4는 주석으로 보존한다.
- `SeedGreedyBench.java`는 실험 B 모드다.
- 추가 측정은 필요 없다. 비교 문서용 데이터는 완비했다.
- 엔진 최종 활성 버전은 미결정이다. 다음 세션의 결정 사항이다.

### 5.3 improve() fork-join 병렬화 (P0-1 ③)

#### 2차 세션 (2026-07-21). 설계 논의 착수와 fork-join 기초 학습

엔진 코드는 바꾸지 않았다.

- **설계 결정(사용자 합의): 후보 평가를 무상태로 만드는 방법은 A안을 먼저 한다.**
  - A안: 스레드(잎 작업)마다 teams 복사본을 주고 기존의 swap, 평가, 복구 로직을 재사용한다. 계산 순서가 순차와 같아 결과가 비트 단위로 동일하다.
  - A안으로 구현, 검증, 측정까지 마친 뒤에 B안을 별도 단계로 검토한다. B안은 스캔 시작 때 팀별 합과 분산을 스냅샷으로 잡고, 교환에 관여한 두 팀만 같은 순서로 다시 계산하는 방식이다. 역시 비트 단위 동일성을 유지할 수 있고, 사실상 P0-1 2번(증분 재계산)의 팀 단위 한정한 것이다.
  - C안(팀별 합과 제곱합을 저장하고 O(1)로 갱신)은 계산 경로가 달라져 결과 동일성을 깨뜨릴 수 있어 이번 작업에서 제외했다.
- 분할 단위 제안(당시 논의 중이고 미확정): 팀 짝 (a,b) K(K−1)/2개를 한 줄로 늘어놓고 구간으로 나눈다. 정원이 같아 짝당 후보 수가 정원의 제곱으로 균등하다. 잎 작업은 복사본 1개를 만든 뒤 담당 짝을 전수 평가하고 부분 최선 1건을 반환한다.
- fork-join 학습(사용자의 ForkJoinPool 첫 학습, 실습 완료). 실습 파일 3개는 `src/test/java/com/exercisemanagement/practice/forkjoin/`에 있다.
  - SumTaskFlatPractice: 평면 구조이고 재귀가 없다.
  - SumTaskPractice: 재귀이고 역할별로 메서드를 나눴다.
  - MaxTaskPractice: 연습 문제다. 사용자가 직접 완성하고 실행해 검증했다. 최댓값 9의 위치가 4이고, 동점 규칙이 스레드 배정이나 계산 순서와 무관하게 유지됨을 확인했다.
  - 학습 보조로 실행 흐름 19단계 도식을 만들었다(Cowork 아티팩트 forkjoin-workflow-stepper).
- 당시의 다음 세션 복귀 지점은 설계 논의 주제 3번(결정적 축약)이었다. 대기 중이던 질문은 "왼쪽과 오른쪽의 부분 최선을 순차 코드의 규칙(1e-12를 넘는 개선, 동률이면 ID 사전순) 그대로 비교해 병합하면 순차 실행과 항상 같은 최종 후보가 나오는가"였다. 세션 프롬프트는 `docs/병목개선/세션프롬프트_improve_forkjoin_병렬화_2차.md`다. (삭제됨)

#### 3차 세션 (2026-07-22, 2차 프롬프트 실행). 설계 확정과 구현 착수

- **설계 확정**
  - 병렬화 범위는 스캔 1회의 후보 전수 조사만이다. while 반복은 순차로 둔다.
  - 분할 단위는 팀 짝 K(K−1)/2개를 배열 pairA와 pairB(p번 짝의 두 팀)로 펴고, 번호 구간 [lo, hi)를 RecursiveTask 재귀로 반씩 나누는 방식이다.
  - 잎 작업은 전체 teams의 복사본으로 담당 짝만 원본 몸통 그대로 평가한다. A안이라 바깥과 팀 리스트만 새로 만들고 Member는 불변이라 공유한다. 결과는 부분 결과(ImproveTaskResult = 값 1개, 위치 4개, 키 2개)로 반환하고, 후보가 없으면 bestTeamA를 -1로 둔다.
  - 병합은 순차 규칙 그대로다. 오른쪽이 1e-12를 넘게 작을 때만 이기고, 차이가 ±1e-12 안이면 ID 키 사전순으로 정하며, left는 앞 구간으로 고정한다.
- 주제 3번(병합 결과가 순차 결과와 같은가)은 이론 논증 대신 동일성 검증 프로그램 실행으로 확인하기로 결정했다. 구조 선택(주제 4번)은 재귀 스타일로 사실상 확정했다.
- 엔진 파일 상태: 원본 improve()를 주석으로 백업하고, 활성 improve()는 병렬 구현(작성 중)으로 두었다. 완성형 참고용 초안은 전체 주석으로 adopt() 앞에 보존했다. 사용자 주석 정석화를 1회 했고 코드는 바꾸지 않았다.
- 점검에서 문제 10건을 발견했다. A는 옛 구조의 잔재, B는 결과와 병합의 버그, C는 정리 항목이다. A-1에서 A-3까지와 A-4 일부를 반영했다. 남은 것은 A-4의 잔여 6곳, A-5, B-1에서 B-4, C이고 상세 체크리스트는 3차 프롬프트에 있다.
- 학습 보조: Cowork 아티팩트 improve-pair-split-stepper. 팀 리스트를 반으로 나누면 교차 짝이 사라지는 문제와 짝 목록을 나누는 해법을 9단계 도식으로 만들었다.
- 세션 프롬프트: `docs/병목개선/세션프롬프트_improve_forkjoin_병렬화_3차.md` (삭제됨)

#### 4차 세션 (2026-07-22, 3차 프롬프트 실행). 구현 완료와 동일성 검증

수정 체크리스트를 전 항목 완료했고 컴파일도 통과했다. 동일성 검증 40케이스가 전부 통과했다. 측정 계획 문서를 썼고 4절 예측만 남겼다.

- 체크리스트 완료 내역
  - A-4: 잔여 6곳을 지역 변수로 전환했다.
  - A-5: workSize. 사용자가 세션 전에 먼저 반영했다.
  - B-1: 위치 4개를 -1로 초기화했다(기본 생성자).
  - B-2: bestNewDistDev를 지역 변수로 복원하고 결과에 담았다.
  - B-3: 병합에서 동률일 때 2차 비교로 bestKeySecond를 쓴다.
  - B-4: null 검사를 bestTeamA == -1 검사로 바꿨다.
  - C 정리: fork, compute, join 패턴을 정돈했고, RecursiveTask<ImproveTaskResult> 제네릭을 써서 캐스팅을 없앴다. 죽은 지역 변수와 println을 제거했고 주석을 구간 기준으로 갱신했다.
  - 항목마다 숫자로 직접 계산해 보이며 설명했고, 사용자가 직접 수정하는 방식을 유지했다.
- 엔진 추가 변경(사용자 위임으로 Claude가 직접 반영)
  - improve, seedSerpentine, sumDeviation, distributionDeviation을 package-private으로 바꿨다. 검증 프로그램에서 접근하기 위해서이고 seedGreedy와 같은 방식이다.
  - 주석 백업을 복원한 참조 구현 `improveSequential`을 새로 만들었다. 백업 주석은 그대로 두었고, 검증 완료 후 제거 후보다.
- 동일성 검증: `src/test/.../challenge/service/ImproveEquivalenceCheck.java`(main)
  - 부하 5조합 곱하기 시드 2개 곱하기 허용폭 2종 곱하기 시작 배정 2종으로 40케이스이고, 케이스마다 병렬을 3회 반복했다.
  - 판정 기준은 배정이 완전히 일치하고 sumDev와 distDev가 비트 단위로 같은 것이다(doubleToRawLongBits).
  - 40케이스 전부 통과했고 136초 걸렸다. 이로써 주제 3번(병합 결과가 순차 결과와 같은가)은 실측으로 끝냈다.
- 검증 프로그램에 순차와 병렬의 시간을 나눠 출력하는 기능을 넣었다(간이 측정 겸용). 시간 출력 기능은 아직 실행하지 않았다.
- 측정 계획: `docs/병목개선/improve_forkjoin_측정계획.md` (삭제됨)
  - 질문 A는 배율, B는 부하별 변화, C는 교차점과 역전, D는 코어 수 대비 해석이다.
  - 측정 단위는 improve() 1회다. greedy 시작으로 고정하면 같은 입력에서 스캔 횟수가 같아 공정하다.
  - 입력은 정원 10, 시드 42, 허용폭 5%로 고정하고, 주 그리드는 N=60/120/200/300, 소형은 N=20/40이다.
  - 도구는 0단계 간이 측정(방향 확인만 하고 문서에는 넣지 않음) 다음에 1단계 JMH ImproveBench(문서용)다.
- 학습 방침 확정: fork-join 내부(워커 배분과 work-stealing 상세)는 미리 읽지 않는다. 예측과 실측이 어긋나고 그것이 원리 수준으로 설명되지 않을 때만 표적 학습한다. 사용자는 fork-join을 인터페이스 수준으로 이해한 상태이므로, 결정마다 이유를 숫자로 계산해 보이며 진행한다.
- 부수 사항: 프로젝트 루트의 `_to_delete/` 폴더에 세션 중 파일 전송용 스냅샷이 남아 있다. 작업과 무관하고 사용자가 폴더째 삭제할 예정이다.
- 세션 프롬프트: `docs/병목개선/세션프롬프트_improve_forkjoin_병렬화_4차.md` (삭제됨)
- 이 세션의 복귀 지점이었던 "측정 계획 4절 예측"은 5차 세션의 결정으로 무효가 됐다.

#### 5차 세션 (2026-07-23). 측정 실행 계획서 작성

엔진과 측정 코드는 바꾸지 않았다. 문서 작업만 했다.

- **4절 예측 방식 폐기.** 4차 측정계획(`improve_forkjoin_측정계획.md`)의 "코어 수 확인 후 배율을 직접 계산해 예측" 단계를 사용자 결정으로 접었다. 실행 중심으로 다시 쓴 `docs/병목개선/improve_성능측정_실행계획서.md`로 대체했고, 4차 측정계획은 참고용으로 남겼다. 두 문서 모두 이후 삭제됐다.
- **방침 확정(이 세션의 메타 결론)**
  - 계획은 방향과 확인 항목만 잡고, 상세 절차는 실측하면서 정한다.
  - 측정의 중심은 배율 하나다.
  - 예측과 크게 어긋날 때만 계획서 1.4의 관측 항목을 표적으로 꺼낸다. 증상 없이 미리 파지 않는다.
  - 6개 Phase는 목적과 확인 항목만 남기고 상세 체크리스트는 뺐다.
- **고정 입력 변경: 허용폭 5%에서 10%로.** 나머지(정원 10, 시드 42, greedy 시작)와 부하 그리드(주 N=60/120/200/300, 소형 20/40)는 4차와 같다.
- 측정 대상 2개는 `improveSequential`(분모)과 `improve`(분자)다. 측정 단위는 improve() 1회다.
- JMH 설정: AverageTime, ms, fork 2회 이상, `@Setup(Level.Invocation)`으로 복사를 분리, `@Param`으로 N.
- 새 벤치 위치와 패키지 확정: `ImproveBench.java`를 `src/jmh/java/com/exercisemanagement/challenge/service/`에 두고 package는 `com.exercisemanagement.challenge.service`다. SeedGreedyBench와 같은 소스 세트라 package-private 엔진에 접근할 수 있다. 이 시점에는 아직 쓰지 않았다.
- 부수 사항: 실무 fork-join 테스트 항목, 도구, 측정 기준을 정리한 포트폴리오 참고 문서를 사용자에게 따로 전달했다(프로젝트 폴더 밖).
- 세션 프롬프트: `docs/병목개선/세션프롬프트_improve_forkjoin_병렬화_5차.md` (삭제됨)

#### 6차 세션 (2026-07-27). 측정 환경 원복과 스레드 수 스윕 코드 반영

컴파일은 통과했고 측정은 실행하지 않았다. 7차에서 실행한다.

- 원복 내역
  - 엔진 workSize를 원래 식으로 복원했고 행 단위 변수 설명 주석을 추가했다.
  - ImproveBench의 `@Param` 전체 그리드를 복원했고 이전 설정은 주석으로 보존했다.
  - build.gradle의 `profilers = ['gc']`를 주석 처리했다.
- 스윕 코드
  - 엔진에 `improve(teams, tolerance, parallelism)` 오버로드를 추가했다. 2인자 메서드은 위임만 한다. workSize는 논리 프로세서 12 기준으로 고정이라 병렬도 1부터 12까지의 실행에서 분할, 복사, 병합 작업량이 같다.
  - 프로브를 확장했다. N=200 고정, 병렬도 1부터 12까지 스윕, procCPU를 누가 썼는지 나누는 계측(main, 워커, 기타 Java, jvm 잔여, 그리고 GC와 JIT), 13열 출력이다.
- 말투 규칙 갱신: "사용 주체 구분", "재료", "태어난다", "손추적"을 금지했고 1절에 반영했다. 코드 반영 직후 보고 기준도 추가했다(수정 지점 전부를 코드로 보고).
- 다음 세션 복귀 지점: 스윕 실행부터. 세션 프롬프트는 `docs/병목개선/세션프롬프트_improve_forkjoin_병렬화_7차.md`다.

#### 2026-07-28 학습 세션. 측정은 실행하지 않았다

프로브의 GC·JIT 열을 읽기 위한 배경 학습만 했다. 엔진과 측정 코드의 동작은 바꾸지 않았고, 프로브 주석과 이 문서만 손댔다.

- 프로브 주석 반영(코드 변경 없음)
  1. `totalGcTimeMs()` 위 블록 주석을 다시 썼다. 쓰는 곳, `t > 0` 검사 이유, 해석 주의 2가지를 담았다.
  2. 같은 메서드의 for문 위에 `ManagementFactory.getGarbageCollectorMXBeans()`가 무엇인지 항목 단위로 넣었다.
- 학습한 것: 세대 구분과 G1, 옮겨서 회수하는 방식과 단편화, JIT의 배경 수준, MXBean 이름 규칙, JVM 스레드 구성과 세이프포인트. 자료는 8절에 예약으로 정리했다.
- `jvm잔여` 해석에 대한 결정: GC와 JIT 두 항목을 고른 것은 아직 추측이다. 구성을 확정하는 절차는 아래 5단계다.
  1. 스윕 실행 중에 `jcmd <PID> Thread.print`를 1회 떠서 이 기계의 실제 스레드 목록을 확보한다. 프로브가 이미 PID를 찍는다.
  2. 그 목록에서 Java 스레드가 아닌 것을 센다. 이것이 잔여의 구성 후보다.
  3. 순차 행과 병렬 행의 잔여를 비교한다. 순차가 작고 병렬이 크면 잔여 증가분은 병렬화에서 온 것이고, 둘이 비슷하면 improve()와 무관한 배경 활동이다.
  4. 여기까지 나온 것을 들고 도서 2장을 읽는다.
  5. 그다음에 잔여에 대한 결론을 쓴다.
- 7차 측정에서 열별로 다루는 방식을 나눈다.
  1. elapsed·busyCore·speedup·워커별 장부로 확인 항목 4개를 판정한다. 여기까지가 본론이고 GC 지식 없이 된다.
  2. GC 열과 jvm잔여는 값을 기록만 하고 결론을 쓰지 않는다. `-prof gc` 세션에서 8절 자료를 읽은 뒤에 해석한다.
- 아직 재지 않는 관측 후보: 세이프포인트 대기. GC 멈춤 구간은 모든 Java 스레드가 세이프포인트에 도달할 때까지 기다리므로, 워커 하나가 늦으면 나머지가 대기한다. 대기 중에는 CPU를 쓰지 않아 busyCore를 낮춘다. busyCore가 예상보다 낮게 나올 때 `-Xlog:safepoint`로 연다.
- 프로브 개선 후보 2건(미결정)
  1. `getCollectionCount()`를 함께 찍기. GC 시간이 0ms여도 수집은 여러 번 일어났을 수 있다.
  2. 시작 머리말에 JIT 컴파일러 이름과 시간 측정 지원 여부를 한 줄 찍기. 지금은 JIT 열의 0이 "컴파일 없음"인지 "측정 불가"인지 구분되지 않는다.
     - (2026-07-28 후속) JIT 측정 자체를 제외하기로 해서 이 항목은 보류한다.

#### 2026-07-28 프로브 출력 손질. 측정 직전 준비

측정은 실행하지 않았다. 프로브의 출력과 이름만 손봤고, 측정 방식과 값 계산은 바꾸지 않았다.

- JIT 계측을 측정 계획에서 제외했다. 사용자 결정이다.
  - `CompilationMXBean` 관련 줄과 JIT 열은 지우지 않고 전부 주석으로 보존했다.
  - 표는 13열에서 12열이 되었고, 7차 판정 항목 4번은 GC 시간 하나로만 대조한다.
- 표 머리줄에 단위를 괄호로 붙였다. elapsed(ms), busyCore(개), speedup(배) 같은 식이다.
  - `String.format`의 폭은 글자 수로 세는데 한글은 콘솔에서 두 칸을 차지해 머리줄이 값과 어긋났다.
  - 표시 폭을 세어 공백을 채우는 `cell()`과 `printCells()`를 넣어 머리줄과 값의 열 끝 위치를 맞추었다.
- 열 이름 두 개를 한글로 바꾸었다. 볼 때마다 뜻을 다시 따져야 한다는 지적을 받았다.
  - `otherJava`는 기타 자바 스레드로 바꾸었다. main도 워커도 아닌 Java 스레드다.
  - `jvm잔여`는 자바 아닌 스레드로 바꾸었다. procCPU에서 Java 스레드 CPU 합을 뺀 나머지다.
  - 자바 변수 이름은 영문 그대로 두고(`otherJavaMs`, `nonJavaMs`), 필드 주석에 표의 어느 열인지 적었다.
- 기타 자바 스레드 목록 출력을 추가했다.
  - `recordOtherJavaThread()`가 측정할 때마다 id별로 CPU 증가분을 모으고, `printOtherJavaThreads()`가 표가 모두 끝난 뒤 한 번 출력한다.
  - 출력 항목은 id, 이름, 누적 CPU(ms), 잡힌 횟수다. 이번 실행의 모든 N과 병렬도를 합친 값이다.
- 문서 반영 상태
  - 7차 세션 프롬프트에는 위 내용을 반영했다.
  - `improve_forkjoin_오버헤드_측정_정리.md`의 열 이름 표기는 스윕 결과를 반영할 때 함께 고친다.

#### 2026-07-30에서 2026-08-02. 분할 개수 스윕과 결과 논의

측정 결과 두 벌을 읽고 원인 후보를 좁혔다. 엔진과 프로브의 동작은 바꾸지 않았다.

- 스윕 범위는 병렬도 6과 12 두 점, 분할 개수 1부터 12까지와 18, 24, 36으로 15점, 설정 30개를 각각 5회다.
- 실측으로 확정한 것
  1. 평가 횟수 855,000회와 통과 횟수 352,485회가 병렬 설정 30개 전부에서 같다. 일의 양은 변하지 않는다.
  2. 평가 1회당 루프 시간이 분할 1에서 5,192ns, 분할 12에서 18,425ns로 3.55배가 된다. 값을 정하는 것은 워커 수가 아니라 동시에 도는 말단 개수다.
  3. 늘어난 CPU 시간의 99.8%가 워커 안에 있고, 워커는 기다린 것이 아니라 실행 중이었다.
  4. 분할 2에서 3의 급감은 별도 현상이 아니다. 이득 배수만 1에 가까워지고 손해 배수는 1.2 근처로 일정하다.
  5. 원인에서 제외한 것은 일의 양, GC, 기타 자바 스레드, 자바 아닌 스레드, teams 복사, 워커 스레드 개수 자체, 코어가 노는 시간이다.
- 두 편차 계산 메서드의 할당 바이트를 별도 환경의 JDK 21에서 쟀다. 합 편차 계산이 5,760바이트, 분포 편차 계산이 12,320바이트, 평가 1회당 10,839바이트다. 사용자 기계에서 다시 재야 한다.
- 아직 모르는 것은 3.73배 중 CPU 클럭 몫과 메모리 계통 몫의 비율이다. 이번 데이터로는 둘을 나눌 수 없다.
- 다음에 잴 것을 네 단계로 정했다. 1단계 클럭 읽기, 2단계 할당 바이트 측정, 3단계 for문 대조군, 4단계 성능 카운터다.
- 문서 2개
  1. `docs/병목개선/ForkJoinPool/improve_forkjoin_원인추적_관찰과다음테스트_20260802.md`
  2. `docs/병목개선/ForkJoinPool/세션프롬프트_improve_forkjoin_원인추적_20260803.md`
- 말투와 문서 규칙을 이 세션의 지적으로 갱신했다. 영어 용어를 옮겨 새 말을 만들지 않는 규칙과 은유 금지 항목을 더했고, 문서 형식 규칙을 다시 정리했다.

#### 2026-08-03. 클럭 측정 준비와 분할 1 측정

측정 도구를 갖추고 분할 1을 쟀다.

- 엔진 IMPLOG 레코드(leaf·scan) 끝에 시작시각ms 필드를 추가했다. 값은 그 구간이 시작된 시각(System.currentTimeMillis, epoch ms)이고 typeperf 클럭 CSV의 시각 열과 매핑하는 용도다.
- 프로브를 세 가지 수정했다. SPLIT_COUNTS를 분할 1 한 점으로 바꿨고(15점 목록은 주석 보존), 순차와 병렬 설정마다 시작과 끝에 구간 시각 출력을 넣었고, IMPLOG 형식 안내에 시각 필드를 반영했다.
- clocklog_to_xlsx.py를 새로 만들었다. 클럭 CSV와 프로브 콘솔 로그를 읽어 정리 엑셀(클럭 정리, 구간과 스레드 매핑, 스레드별 클럭, 차트, 차트 데이터)을 만든다.
- 분할 1 실측: 순차 4.16, 병렬도 6이 4.23, 병렬도 12가 4.21GHz다. 판정식의 A(동시 실행 1일 때 클럭)를 약 4.2GHz로 확정했다.
- 한계 합의: 클럭 CSV는 논리 프로세서별 값이고 스레드가 어느 프로세서에서 돌았는지는 기록되지 않는다. 스레드별 대응은 활동 창 단위까지이고 값은 그 시각의 전체 프로세서 평균이다.

#### 2026-08-04. 1단계 클럭 측정 완료와 판정

15점(분할 1부터 12까지와 18, 24, 36)을 전부 재서 판정 두 개를 끝냈다. 분할 1과 2는 회차별 로그를 남기도록 고친 프로브로 다시 쟀다.

- 실측 요약(병렬 구간 평균): 동시 실행 1에서 4.25에서 4.26, 2에서 4.23, 3부터 6까지 4.20에서 4.09로 계단처럼 내려오고, 7 이상에서는 4.06에서 4.12 사이에 머물러 더 내려가지 않는다. 순차는 실행마다 4.15에서 4.26 사이다.
- 판정(테스트 절 3·4번): 클럭 배수 A÷B는 4.2 나누기 4.086으로 1.028이고, 클럭만으로 설명되는 값은 4,836 곱하기 1.028로 약 4,971ns다. 실측 12,458ns에 크게 못 미치고, 늘어난 시간 7,622ns 중 클럭이 설명하는 몫은 약 2%다. 클럭은 주된 원인이 아니다.
- 판정(테스트 절 5번): 동시 실행 6과 12의 클럭이 4.086과 4.081로 같다. 12,563에서 18,425ns로 오른 몫은 클럭이 아니라 SMT다.
- 프로브에 회차별 원값 로그(IMPLOG,run)를 추가했다. 측정 반복문 안에서 회차마다 elapsed·procCPU·mainCPU·workerCPU·기타 자바·자바 아닌·GC 원값을 버퍼에 모아 표 행 뒤에 출력한다. 기존 5회 평균 로깅은 그대로다.
- clocklog_to_xlsx.py를 갱신했다. run 레코드를 읽어 회차별 측정값 시트를 추가했고, 스레드별 클럭 시트 main 행의 활동 시간 합을 실측 mainCPU로 바꿨다. 이전의 경과 시간 계산(스캔 바퀴 합에서 말단 루프 합을 빼는 것)은 말단이 2개 이상 겹치면 겹친 시간이 두 번 빠져 음수가 나와 쓰지 않는다.
- clocklog_summary_to_xlsx.py를 새로 만들었다. 폴더의 원본 쌍(CSV와 콘솔 로그)을 전부 찾아 종합, 구간 상세, 회차별 원값 종합, 차트 네 장을 만든다. 같은 분할을 여러 번 다시 잰 경우에는 CSV 수정 시각이 가장 늦은 것을 대표로 쓴다. 판정 계산 시트는 사용자 결정으로 넣지 않았다.
- 원본데이터 폴더(4.ClockTest_원본데이터)에 분할마다 CSV, 콘솔 로그, 클럭정리 엑셀 세 벌씩 남겼다. 분할 1은 _3 파일이고 분할 2는 재측정이 기존 이름을 덮어썼다.
- 미결 셋: 1단계 결과의 관찰 기록 문서(20260802) 반영, 2단계(할당 바이트) 수정 지점 보고와 승인, 프로브 SPLIT_COUNTS 15점 복원(현재 {36}).

#### 2026-08-04 저녁. 할당 실측과 for문 대조, 원인 확정 (프롬프트 20260805 실행)

5단계 계획의 1단계부터 4단계까지를 하루에 마쳤다. 남은 것은 5단계의 결정 사항이다.

**프롬프트 7절 대조**

- 파일별 현재 상태 일곱 항목이 실제 코드와 전부 맞았다. 다른 곳은 없었다.
- 함께 확인한 것 셋. `1.implog_to_xlsx.py`가 새 형식을 못 읽는 이유는 말단 14칸을 13칸만, 스캔 9칸을 8칸만 받고 run 레코드는 처리 갈래가 없기 때문이다. 4.ClockTest 폴더에 엑셀 임시 파일이 남아 있다. ImproveEquivalenceCheck는 2인자 improve()를 부르므로 검증되는 분할 경로가 스윕 경로와 다르다.

**1단계. 프로브에 할당 바이트 계측 추가와 스트림 구현 실측**

- 프로브 한 파일에 열두 곳을 고쳤다. 엔진은 건드리지 않았다. `snapshotAllThreadCpu`가 스레드별 CPU를 읽는 그 자리에서 `com.sun.management.ThreadMXBean.getThreadAllocatedBytes`를 함께 읽고, 증가분을 main·워커·기타로 나눠 담고, [할당] 출력과 IMPLOG run 레코드에 세 칸을 더했다.
- 할당은 CPU 증가분 검사보다 앞에서 더한다. Windows CPU 해상도가 15.625ms라 CPU가 0으로 잡히는 스레드도 할당은 했을 수 있기 때문이다.
- 스트림 구현 실측(분할 36, 병렬도 6과 12, 순차): 총 할당이 순차 9,517.9MB, 병렬도 6이 9,522.1MB, 병렬도 12가 9,521.4MB다. 평가 1회당 11,132에서 11,137바이트다.
- 관찰 문서의 예상은 9,267.4MB와 10,839바이트였다. 실측이 2.7% 크다. 다른 환경 JDK 21에서 가져온 두 값(5,760과 12,320바이트)이 이 기계 JDK 17에서도 거의 그대로 성립한다.
- 순차 구현에는 두 편차 메서드 말고 객체를 만드는 곳이 없다. 그래서 순차 총량이 곧 두 메서드의 몫이고, 병렬이 더 만든 3.44MB가 복사본과 작업 객체와 로그 문자열의 몫이다.
- 2.7%가 두 메서드 중 어느 쪽에서 나온 것인지는 총량 하나로는 나눌 수 없다. 후보로만 남긴다.

**2단계. for문 전환과 동일성 검증**

- 두 편차 메서드를 명령형 for문으로 바꿨다. 스트림 구현 원본은 각 메서드 아래에 주석으로 보존했고 되돌리는 방법도 그 주석에 적었다.
- 중요한 발견: `DoubleStream.sum()`과 `average()`는 단순 누적이 아니라 보정 항을 함께 굴리는 합이다(Kahan 방식). 단순 for문으로 바꾸면 값이 달라진다. 같은 입력으로 팀 400,000개를 재 보니 합 계산에서 162,161건, 평균에서 141,500건의 비트가 어긋났다.
- 그래서 보정 계산까지 그대로 옮겼다. 마지막 단계는 `sum - comp`다. 처음에 `sum + comp`로 썼다가 분산 계산에서 87건이 어긋나는 것을 실측으로 잡았다.
- 검증 결과 두 가지. 메서드 단위로 배정 상태 80,000가지에서 스트림 구현과 비트 불일치가 0이다(JDK 21에서 확인). ImproveEquivalenceCheck 40케이스가 전부 통과했고 81.7초 걸렸다(이전 136초).

**3단계. for문 구현 할당 실측**

- 총 할당이 순차 0.05MB, 병렬도 6이 4.23MB, 병렬도 12가 3.51MB다. 두 메서드의 할당이 사라졌다.
- 병렬에 남은 3.4MB는 스트림 구현에서 병렬과 순차의 차이로 추정했던 3.44MB와 맞는다. 두 메서드만 0이 되고 나머지는 그대로 남았다는 뜻이다.

**4단계. 15점 스윕과 판정**

- 프로브 SPLIT_COUNTS를 15점으로 복원해 설정 30개를 한 번의 실행으로 돌렸다. 말단 레코드 70,200건이다.
- 판정 근거(평가 1회당 루프 시간, 병렬도 6): 분할 1에서 6까지 스트림 구현이 4,836에서 12,458ns로 2.576배가 되는데, for문 구현은 2,688에서 2,736ns로 1.018배다. 단계 배수도 1.14에서 1.22이던 것이 0.99에서 1.02가 됐다.
- 병렬도 12도 같다. 분할 1에서 12까지 스트림 구현 3.549배, for문 구현 1.125배다.
- CPU 시간 총합이 늘어나는 현상도 사라졌다. 스트림 구현은 병렬도 12에서 4,509ms가 14,319ms로 3.18배가 됐는데, for문 구현은 분할을 어떻게 바꿔도 2,144에서 2,453ms 사이에 머문다.
- 배율은 2.72배에서 8.84배가 됐다(병렬도 12, 분할 11, busyCore 10.01). 순차도 같은 조건에서 4,137ms가 2,151ms로 1.92배 빨라졌다.
- 교차 검산: 설정 30개 전부에서 호출 1회당 평가 855,000회와 통과 352,485회가 스트림 구현과 같다. 계산 경로가 바뀌지 않았다는 뜻이다.
- **판정(5절 3-1): 동시 실행이 늘 때 평가 1회에 붙던 1.2배의 원인은 스트림이 만드는 객체, 즉 메모리 쓰기다.**
- 예상과 다른 관찰 하나: 5절 4번은 동시 실행 6에서 12 구간의 배수는 SMT 몫이라 for문 구현에서도 남는 것이 정상이라고 봤는데, 1.467배가 1.035배로 거의 남지 않았다. 확정할 수 없어 후보로만 적는다.
- 새로 드러난 것 둘: 병렬도 6에서 분할 6이 410.3ms인데 분할 7이 647.0ms로 1.58배 나빠진다(스트림 구현에서는 1.123배로 보였다). 병렬도 12에서도 분할 11이 243.3ms인데 분할 12가 308.9ms다.

**스크립트**

- `clocklog_to_xlsx_v2.py`를 만들었다. run 레코드의 할당 세 칸을 읽어 회차별 측정값 시트에 할당 열 여섯 개를 만든다. 표본이 0개인 구간의 평균 클럭은 빈 칸으로 둔다(for문 구현은 회차가 0.3초 안팎이라 1초 간격 표본이 걸리지 않는다).
- `implog_to_xlsx_v2.py`를 새로 만들었다. 새 레코드 형식을 읽고, 판정에 쓰는 배수 두 열(최소 분할 기준, 직전 분할 대비)을 넣고, 회차별 원값 시트를 만든다. 옛 형식도 그대로 읽힌다.
- 두 스크립트와 사용법은 `docs/병목개선/ForkJoinPool/기타/원본데이터/5.ClockTest_원본데이터_v2_메모리할당량계측추가/`에 있다.

**5단계 결정 사항**

1. for문 구현은 유지하지 않는다. 두 편차 메서드를 스트림 구현으로 되돌리고, 자원이 왜 그렇게 쓰였는지 추적을 이어간다. 다음 도구는 JFR이고 9.5GB가 어느 줄에서 나오는지 본다.
2. 증분 재계산(P0-1 2번)은 사용자가 로직을 다시 확인한 뒤에 다룬다. 시점은 추적이 끝난 다음이다.
3. 판정 결과의 기록 문서 반영은 사용자가 데이터 패턴을 보고 의논한 뒤에 한다. Claude가 먼저 초안을 쓰지 않는다.
4. ImproveEquivalenceCheck 삭제는 미룬다. 삭제하려면 for문 구현을 그 프로그램 기준으로 다시 검증해야 하고, 그 결론은 for 전환에 대한 결론이 선 뒤에 나온다.
5. 계측 두 벌과 IMPLOG 시각 필드, 프로브의 할당 계측, `pool.shutdown()` 주석은 문서용 JMH 확정 측정 전까지 그대로 둔다.

**busyCore 판정 (추가 실행 없이 기존 데이터로 닫았다)**

- busyCore는 procCPU를 elapsed로 나눈 값이다. 워커가 일하는 동안만 CPU 시간이 쌓이는데 elapsed는 그 스캔에서 제일 늦게 끝난 워커가 정한다. 그래서 일이 고르게 나뉘지 않으면 elapsed만 길어지고 busyCore가 내려간다.
- 실측으로 확인했다. 병렬도 6에서 busyCore와 불균형의 곱이 5.84에서 6.18 사이이고 전부 워커 수 6 근처다. 병렬도 12도 같다. 분할 11에서 10.01 곱하기 1.07로 10.7이고 그때 실제로 일한 워커가 11명이다.
- 닫힌 것은 busyCore라는 열을 어떻게 읽는가까지다. 값이 안 오르는 것은 코어가 모자라서가 아니라 워커가 놀아서다.
- 불균형 값 자체는 역계산이 아니다. leaf 원본에서 스캔 한 바퀴마다 워커별로 4중 루프 시간을 직접 더해 최댓값을 평균으로 나눈 값이다.

**새로 열린 질문: 왜 일이 고르게 나뉘지 않나**

- 분할 7은 산술로 확정이다. 말단이 7개인데 워커가 6명이면 누군가는 두 개를 맡는다. 운영체제 스케줄러와 무관하다.
- 분할 12에서 실제로 일한 워커가 11.4에서 11.8명인 것은 미확정이다. fork-join이 일감을 다 나눠 주기 전에 다른 워커가 먼저 집어 간 것인지, 일감은 갔는데 운영체제가 그 스레드를 코어에 못 올린 것인지 지금 데이터로는 구분되지 않는다.
- 구분 방법: 말단 레코드에 담당 짝 구간(`lo`, `hi`)과 `System.nanoTime()`을 더하면 스캔 한 바퀴의 워커별 활동 구간을 시간축에 늘어놓을 수 있다. 애초에 일감이 안 갔으면 배분 문제이고, 갔는데 시작이 늦었으면 운영체제 쪽이다. 후자가 의심되면 JFR의 스레드 park 이벤트를 붙인다.
- 이 항목의 근거는 오늘 얻은 불균형 수치다. 분할 7의 1.65배, 분할 12에서 워커 하나가 노는 현상, 분할 36에서도 남는 1.11배가 설명 대상이다.

**다음 작업 목록 (2026-08-04 합의)**

작업 여섯 개를 번호 순서대로 하나씩 한다. 사용자 지시로 두 작업을 한 번의 수정이나 한 번의 실행으로 합치지 않는다.

1. 두 편차 메서드를 스트림 구현으로 되돌린다. 되돌리는 방법은 각 메서드 아래 보존 주석에 있다. 이 작업에서는 실행하지 않는다.
2. 할당이 어느 줄에서 나오는지 확인한다. 분할 개수를 한 점으로 줄이고 JFR을 켠 채 한 번 실행해 클래스별 순위와 할당 지점을 본다. 시간 값은 판정에 쓰지 않는다.
3. 말단 기록에 담당 짝 구간과 나노초 단위 시각을 더한다. 추출 도구도 함께 고친다. 이 작업에서는 스윕을 돌리지 않는다.
4. 일감 배분을 실측한다. 기록 도구를 끄고 15점 스윕을 돌려, 분할 12에서 노는 워커에게 일감이 갔는지를 본다.
5. 일감 훔치기 방식의 자료를 읽고 예측을 먼저 적은 뒤 작업 4의 실측과 맞댄다. 며칠짜리다. 포트폴리오에서 이 항목의 값은 원리를 읽고 그 예측을 실측으로 확인했다는 점이다.
6. 증분 재계산(P0-1 2번)을 진행할지 정한다. 사용자가 로직을 먼저 확인한다.

각 작업의 절차와 끝 기준은 세션 프롬프트 4절에 작업별로 나눠 적어 두었다.

#### 2026-08-05. 스트림 구현 복귀와 JFR 할당 지점 확인 (프롬프트 20260805의 작업 1·2 실행)

위 목록의 작업 1과 2를 마쳤다. 남은 것은 작업 3부터이고, JMC 직접 분석 마무리가 그 앞에 있다.

**프롬프트 7절 대조**

- 파일별 현재 상태 일곱 항목이 실제 코드와 전부 맞았다. 다른 곳은 없었다.

**작업 1. 두 편차 메서드를 스트림 구현으로 복귀**

- 수정 지점 보고와 승인을 거쳐 두 메서드를 스트림 구현으로 되돌렸다. 합 스트림이 1230행, 평균 스트림이 1278행, 분산 스트림이 1280에서 1284행이다.
- for문 구현은 각 메서드 아래 [보존 · for판] 주석 블록으로 보존했다. 되돌리는 방법, 보정 계산 주의(마지막 단계가 sum - comp 뺄셈), 검증 이력을 그 주석에 적었다.
- 이 작업에서는 실행하지 않았고, 컴파일 통과는 사용자가 확인했다.

**작업 2. JFR로 할당 지점 확인**

- 프로브 설정을 한 점(병렬도 6, 분할 36)으로 줄였다. 승인 후 반영했고 15점 목록과 {6, 12}는 주석 보존이다. 직전 할당 실측과 조건을 맞추기 위한 선택이고, 총 할당량이 병렬도와 분할에 무관함은 기존 실측이 근거다.
- 사용자가 IntelliJ 실행 구성 VM options에 `-XX:StartFlightRecording=settings=profile,filename=improve_alloc.jfr`을 넣고 한 번 실행했다.
- 복귀 교차 확인: [할당] 출력이 호출 1회당 9,522.0MB로 8월 4일 실측 9,522.1MB와 일치한다. 평가 855,000회와 통과 352,485회도 같다.
- 표본 방식 이벤트(Object Allocation Sample) 11,568개를 jfr print와 파이썬으로 집계했다. 스택을 전체 깊이로 뽑으면 엔진 프레임이 없는 표본이 0.00%다.
- **판정: 9.5GB는 스트림을 생성하는 세 문장에서 나온다.** 표본 비중은 합 스트림(1230행) 52.3%, 평균 스트림(1278행) 25.2%, 분산 스트림(1280·1281·1284행) 22.5%이고, 작업 객체 생성 등 나머지는 0.03%다.
- 조각 값 대조가 맞았다. 메서드 몫은 예상 53.1 대 46.9에 실측 52.3 대 47.7이다. 클래스 구성도 목록을 훑는 도구 10.3%(예상 10.8), 스트림 시작 19.4%(18.9), 변환 단계 19.6%(18.9), 합계·평균 실행분 50.7%(51.6), mean을 담는 캡처 람다 1.9%(1.7)로 일치한다.
- 캡처 람다의 표본은 전부 분산 스트림을 만드는 1280행에 붙어 있다. 평균 쪽 람다는 바깥 값을 안 가져와 재사용되므로 나타나지 않는다.
- 교차 확인 두 가지. 기록 전체의 추정 총량 140,659MB는 이번 실행의 호출 15회 몫(142,830MB)의 98.5%다. main과 워커의 나눔도 순차 7회와 병렬 8회의 몫과 각각 맞는다.
- 2.7% 차이(실측 총량 대 조각 값 계산)가 어느 메서드 몫인지는 표본 정밀도 밖이라 후보로 남긴다.

**학습과 도구**

- TLAB와 포인터 밀기(bump-the-pointer)를 학습했다. 학습 보조 아티팩트는 bump-pointer-stepper다.
- JMC 9.1.2를 `E:\devSpace\Tools\org.openjdk.jmc-9.1.2-win32.win32.x86_64\`에 두었다. JDK 21이 있어야 돌아서 Temurin 21을 PATH에 넣지 않고 설치했고, jmc.ini 맨 위 -vm 두 줄로 지정했다. 명령창의 기본 java는 17 그대로다.
- 사용자가 JMC에서 기록 파일을 열어 Memory 화면까지 확인했다. 직접 분석은 다음 세션에서 잇는다.
- 원본: improve_alloc.jfr은 프로젝트 루트, 실행 콘솔 로그는 `docs/병목개선/ForkJoinPool/기타/원본데이터/6. improve_ForkJoinPool_JFR측정/`에 있다. (2026-08-06 정리로 정식본은 그 폴더의 1_1 파일이 되었고 루트 사본은 _to_delete로 옮겼다.)

#### 2026-08-06. JMC 직접 분석 마무리와 원본 정리 (프롬프트 20260806의 할 일 1·2 실행)

할 일 1(JMC 직접 분석 마무리)과 2(집계 스크립트와 원본 정리)를 마쳤다. 남은 것은 할 일 3(말단 기록 보강)부터다.

**프롬프트 7절 대조**

- 일곱 항목 중 여섯이 실제와 맞았다. 다른 곳 하나는 8절의 2026-08-05 TLAB 학습 항목이 실제로는 없다는 것이었다. 자료 목록이 지난 세션에만 있어 채울 수 없었고, 사용자 결정으로 5.3절의 해당 참조 문장을 지워 정리했다. jmc.ini는 접근 범위 밖이라 미확인으로 남겼다.

**할 일 1. JMC 직접 분석 마무리**

- 사용자가 Memory 화면에서 직접 확인했다. 스택 프레임 구분을 Line Number로 바꿔 줄 번호 단위로 보는 방법을 익혔다.
- 확인 결과: 클래스 표의 순위와 비중이 지난 세션 집계와 일치했다. 줄 위치는 변환 단계 객체(ReferencePipeline$6)가 1230·1278·1281행, 시작 객체(Head)가 1230·1278·1280행, mean을 담는 람다(TeamFormationEngine$$Lambda)가 1280행뿐(1278행 0건), double[]이 1230·1278·1284행으로, 세 문장의 모든 줄이 화면으로 확인됐다.
- 스택을 바깥으로 펼치면 코드의 실제 호출 지점(순차 구현 후보 평가, 병렬 말단의 후보 평가, 스캔 기준값 계산, 프로브의 워밍업과 측정 반복문)과 일치했다. 워밍업 대 측정의 표본 비 219 대 582가 실행 횟수 비 2 대 5와 맞았다.
- 학습한 것: 표본 방식(TLAB이 다 차는 순간 뽑히고 무게로 합산), 달러 붙은 클래스 이름 작명 규칙, 스택 트레이스 읽는 법, 스트림의 지연 실행과 일회용 성질이다. 표본 개수는 스레드에 따라 치우칠 수 있어 몫 비교는 무게로 한다는 주의도 실측으로 확인했다(main 무게 47.4% 대 표본 개수 67%).

**할 일 2. 집계 스크립트와 원본 정리**

- 결정 셋: 집계 스크립트는 재작성해 원본데이터에 넣는다(지난 세션 스크립트는 보존되지 않았다), 루트 improve_alloc.jfr은 지운다(6번 폴더의 1_1 사본이 정식), 6번 폴더 안에 실수로 남은 JMC 설치본 사본은 정리한다. 루트 jfr과 JMC 사본은 _to_delete로 옮겼고 사용자가 폴더째 삭제할 예정이다.
- 집계 도구를 6번 폴더에 만들었다. `2. jfr_alloc_집계.py`와 `2_1. 사용법`이다. jfr print(--stack-depth 64)로 뽑은 텍스트를 읽어 엑셀(`2_2.`, 시트 5장: 클래스별, 스택 트레이스, 문장별, 스레드별, 판정 요약)을 만든다. 단위는 JMC 화면과 같은 표기(GiB, Samples)이고, 카운터 실측과 대조하는 자리에만 MB를 함께 적는다.
- 다시 만든 스크립트 검증: 같은 기록 파일에서 표본 11,568개와 비중(52.3/25.2/22.5, 클래스 구성)이 지난 세션 집계와 일치했고, 총량은 140,674MB로 0.01% 차이(무게 반올림 몫)다. main 몫 66,658MB가 순차 7회 몫 계산 66,655MB와 겹치는 것도 확인했다.

**문서 두 개 작성**

- `improve_forkjoin_JFR할당지점_분석보고서_20260806.md`: 판정, 총량 교차 확인(98.5%), 문장별·클래스별 대조, JMC 직접 확인 여섯 가지, 스레드별 나눔, 후보(2.7% 차이)를 담았다. 위치는 원본데이터 6번 폴더다. 판정 결과의 문서 반영은 이 보고서 작성으로 처리했다.
- `JMC_사용법과_배경지식_20260806.md`: 실행 환경과 기록 만들기, Memory 화면 구성, 표본 방식·클래스 이름·스택 읽는 법. 위치는 6번 폴더의 기타 아래다.
- 두 문서 모두 사용자가 직접 확인하고 이해한 범위로만 썼다. JDK 내부 구현 수준의 내용은 넣지 않았다.

**세션 말미 결정 (문서 재편)**

- 지금까지의 문서가 시간 순서로 쌓여 타임라인과 데이터가 여러 파일에 나눠 있다는 문제로, 사용자가 문서 체계를 다시 잡기로 했다.
- 사용자가 기존 세션 프롬프트 전부와 JFR 분석 보고서를 지웠다. 남은 취합 대상은 ForkJoinPool 폴더 바로 아래의 md 아홉 개다.
- JFR 측정 데이터는 `improve_forkjoin_JFR측정_데이터소스_20260806.md`로 전량 보존했다(집계 값, JMC 스택 확인 상세, 산술 대조, 한계점 여덟 가지).
- 다음 세션은 문서 취합과 최종 보고서 작성이다. 원본데이터 폴더는 절대 수정하지 않는다. 측정 작업(말단 기록 보강부터)은 문서 작업 뒤에 재개한다.

#### 2026-08-07. 문서 취합과 성능 분석 네 편 작성

취합 전 보고서 아홉 개를 성능 분석 네 편과 측정 데이터 소스 한 편으로 다시 썼다. 엔진과 측정 코드는 건드리지 않았다.

**만든 문서**

- 값이 나오는 문서를 둘로 나눴다. 판단과 근거를 담는 분석 문서와, 회차별 측정 조건과 값 전량을 담는 데이터 소스 문서다.
- 데이터 소스는 `improve_forkjoin_측정_데이터소스_20260807.md`다.
- 분석 문서는 처음에 한 파일로 썼다가 사용자 결정으로 네 편으로 나눴다. 이름은 0절의 취합 결과 행에 있다.
- 회차 이름을 일곱 개로 정했다. 기준 측정, 분할 개수별 측정, 카운터 측정, 상세 기록 측정, 클럭 측정, for문 대조 측정, JFR 측정이다. 본문에서 값을 적을 때 어느 회차인지 이 이름으로 밝힌다.
- 클럭 측정의 값과 판정, JFR 측정의 판정은 네 편에 넣지 않았다. 최종 보고서에서 다룬다.

**누락 점검**

- 취합 전 문서 아홉 개와 취합 결과를 대조해 빠진 것 여덟 개를 찾았다.
- 사용자가 그중 넷을 넣기로 했다. 효율 곡선, 동일성 검증 40케이스의 구성과 판정 기준, 카운터 회차의 관찰 둘과 미결 질문 하나, 워커별 CPU 개별 값 목록이다.
- 보류한 넷은 false sharing 후보 제외, 객체 하나가 캐시 덩어리를 넘어갈 때의 시간 82.9ns와 72.1ns, 워커 생성과 일감 훔치기 비용을 계산으로 제외한 것, 계측 관리 방침과 현재 주석 상태다.

#### 2026-08-07 이어서. 3편과 4편 재작성, 문서 작성 지침 재작성

같은 날 세션에서 3편과 4편을 폐기하고 처음부터 다시 썼다. 엔진과 측정 코드는 건드리지 않았다.

**형식을 바꾼 이유**

- 다시 써도 읽히지 않는다는 지적이 이어졌고, 원인을 찾아보니 이 문서의 옛 9.2절이 서술형 문단을 예외 없이 금지하고 있었다. 설명이 들어갈 자리가 없어 값과 항목만 남았고, 기계 점검은 그 형식을 지켰는지만 봤다.
- 문단 중심으로 3편 1절을 다시 써서 사용자 승인을 받았다. 그 글이 지금 1.3절의 기준 예시다.
- 항목을 문단으로 바꾸면 길어질 것으로 봤는데 실제로는 짧아졌다. 다시 쓰기 직전에 두 형식의 글자 수와 줄 수를 재서 확인했고, 번호 항목은 3편에서 하나도 남지 않았다.

**클럭 측정의 자리 (최종 보고서에서 쓸 것)**

- 클럭 측정은 잰 시점에는 필요했다. 8월 2일까지 나온 상태가 평가 1회의 3.73배 중 클럭 몫과 메모리 계통 몫을 나눌 수 없다는 것이었고, 엔진을 고치지 않고 후보 하나를 지울 방법이 클럭 직접 측정이었다.
- 다만 판정을 떠받치는 것은 for문 대조다. for문 구현도 말단 12개를 동시에 돌리므로 클럭이 내려가면 같이 내려가는데, 분할 1에서 12로 갈 때 2,515ns가 2,828ns로 1.12배에 그쳤다. 클럭이 설명할 수 있는 최대치가 1.12배라는 뜻이라 클럭 측정이 낸 2%보다 상한이 좁다.
- 그래도 클럭 값은 버리지 않는다. 4편 4.4절은 동시 실행 6과 12의 클럭이 4.086과 4.081로 같다는 값이 있어야 성립한다. 그 값이 없으면 그 구간의 1.467배를 하드웨어 몫으로 볼 근거가 없다.
- 최종 보고서에는 두 갈래가 같은 결론에 닿았다는 모양으로 쓴다. 1.8절 7번과 같다.

**이 문서**

- 옛 1절(문서 정독과 지침 준수), 2절(응답 톤과 말투), 9절(문서 작성 표준)을 지우고 1절 하나로 다시 썼다. 날짜별 어긴 사례 기록은 전부 뺐고 규칙만 남겼다.
- 나머지 절 번호를 당겼다. 옛 3절부터 10절까지가 지금 2절부터 8절이다.

#### 2026-08-08. 1편과 2편을 합쳐 다시 쓰고 편 번호를 당겼다

엔진과 측정 코드는 건드리지 않았고 새 측정도 하지 않았다.

**편 번호**

- 사용자 결정으로 합친 편을 1편으로 두고 지금의 3편을 2편, 4편을 3편으로 당겼다. 성능 분석이 네 편에서 세 편이 되었다.
- 파일 이름은 `improve_forkjoin_병렬화성능분석_1.md`와 `_2.md`와 `_3.md`다.
- 옛 1편과 2편은 사용자 결정으로 `ForkJoinPool/_to_delete/`에 `..._1_옛것.md`와 `..._2_옛것.md`로 옮겼다. 사용자가 폴더째 지울 예정이다.

**합친 1편**

- 제목은 "동시에 도는 말단이 늘수록 평가 1회가 느려진다"다.
- 옛 문장을 옮기지 않고 처음부터 다시 썼다. 1절이 말과 값의 이름과 회차, 2절이 기준 측정, 3절이 분할 개수별 측정과 카운터 측정, 4절이 상세 기록 측정의 현상 셋, 5절이 확정한 것과 남긴 것이다.
- 용어는 모아 두되 항목 나열이 아니라 알고리즘과 병렬 구조를 설명하는 문단 안에서 뜻을 밝히는 식이다. 2편과 3편이 "용어와 집계 규칙은 1편에 모여 있다"를 가리키고 있어 모아 두는 자리 자체는 유지했다.

**최종 보고서로 넘긴 것**

- 보류한 누락 항목 넷(false sharing 후보 제외, 82.9ns와 72.1ns, 워커 생성과 일감 훔치기 비용 제외, 계측 관리 방침)은 사용자 결정으로 최종 보고서로 넘겼다.

**남은 것**

- 3편 앞머리 표가 가리키는 `improve_forkjoin_JFR측정_데이터소스_20260806.md`는 파일 이름만 적혔는데 실제로는 `기타/보고서_취합전원본데이터들/` 아래에 있다. 파일을 옮길지 경로를 적을지 정해야 한다.

#### 2026-08-14. JFR 재측정 1회차 분석, 측정 종료 결정, 엔진 정리

**1회차 JFR 분석 (8번 폴더).** 증분 재계산 구현의 할당 기록(병렬도 6, 분할 36, 워밍업 100회)을 JMC 화면과 jfr print 표본 집계 두 방법으로 읽었고 행 번호와 비중이 전부 일치했다. 워커 할당은 말단 compute() 안의 다섯 자리다. teams 깊은 복사(1160·1158행, 약 70%), 배열 사본 clone 두 줄(1165·1166행, 14.4%), ImproveTask 생성(1121·1124행), 상세 기록 문자열(1292행, 계측 몫이라 분석 제외), ImproveTaskResult(1174행). 스트림 클래스는 표본 0개다. 결과 문서는 사용자 결정으로 표준 보고서 구조(문서 정보 표, 개요, 방법, 결과, 참고)로 썼고, 이후 ForkJoinPool 폴더로 옮겨 `improve_forkjoin_병렬화성능분석_5.md`가 되었다. 측정 보고서는 앞으로 이 형식을 쓴다.

**for문 기준값 정정.** 세션 프롬프트의 3,420,000바이트는 5번 폴더 15점 스윕 로그의 병렬도 6 분할 36 워커 3,420,237바이트가 원본이다. 같은 폴더 단독 실행의 콘솔 평균 4,128,474바이트는 1회차(6.95MB, 첫 병렬 실행 준비 할당 포함)가 끌어올린 값이라 쓰지 않는다. 회차별 원값 시트에서 2~5회차가 3.420~3.424MB로 확인됐다. 증가분 557,008바이트는 배열 사본 환산 574,330바이트와 3.1% 차이다(계산 대조).

**측정 종료 결정.** 남은 재측정(증분 15점, 스트림 15점, for문 재측정)과 7.1절의 풀 미종료 실험을 하지 않기로 했다. 근거는 카운터 실측이 세 구현의 15점 전 구간을 이미 덮고 있고(분할 1개당 할당 123~140KB로 비례 확인), 할당의 클래스·행 구성은 설정과 무관하며, JFR의 남은 몫은 교차 확인 하나뿐이라는 것이다. main 할당의 출처 확인도 하지 않기로 했다.

**엔진 정리.** 사용자가 주석 원본을 `docs/병목개선/ForkJoinPool/기타/코드백업`에 보관한 뒤, 엔진에서 계측 카운터 넷과 리셋, 상세 기록 필드와 drainDetailLog, IMPLOG 기록 코드 전부, [보존] 주석 여섯 계열, _backup 넷(improveSequential_backup, improveBySplitCount_backup, improve_backup, ImproveTask_backup), for판 보존 두 블록, getStealCount 주석을 삭제했다. `pool.shutdown()`을 되살렸고, improve 오버로드 사슬(2·3·4인자)을 접어 운영 2인자가 workSize를 계산해 본체를 바로 부른다. 2,028줄이 1,088줄이 되었다. seedGreedy 쪽 알고리즘 코드와 계수 계측은 건드리지 않았다. improveSequential(참조 구현)은 남겼다. 프로브와 ImproveEquivalenceCheck는 엔진 계측을 부르는 줄만 [비활성 2026-08-14] 표시로 주석 처리해 온존했다. ImproveEquivalenceCheck의 대조1(새 순차 대 새 병렬)은 살아 있다.

**검증.** 정리판을 컨테이너(JDK 21)에서 컴파일 통과와 ImproveEquivalenceCheck 40케이스 전부 통과(배정 불일치 0건)로 확인했다. 옛판 대조가 빠져 실행 시간은 98초에서 7초 규모가 되었다. 사용자 기계(JDK 17)에서 컴파일과 40케이스 1회 재확인을 권한다.

**파일 취급 결정.** `docs/병목개선/ForkJoinPool/기타` 아래는 테스트 데이터이자 포트폴리오 근거라 삭제·수정하지 않는다(1_3 문서의 문구 수정만 사용자 승인으로 예외). 프로젝트 루트의 hotspot 로그 둘과 jfr 둘도 건드리지 않는다.

---

## 6. 다음 작업 계획

2026-07-14 합의, 2026-07-15 갱신. 우선순위 순이다.

1. ~~7.4 분기 계수 실험~~ 2026-07-15 완료. 5.2절 참조.
2. ~~K축 배율 이탈 원인 논의~~ 2026-07-15 완료.
   - 결론: 원인은 인원당 고정 비용이다. 바깥 for는 인원당 1회 실행이고 K와 무관한데, 이 비용이 작은 K 구간에서 큰 비중을 차지한다(K=3에서 약 80%).
   - 알고리즘은 여전히 O(N·K)다. 운영급 K(수천에서 수만)에서는 고정 비용 비중이 0.1% 미만이라 사실상 사라진다.
   - K 스윕(N 고정, K 6점)은 하지 않기로 결정했다. 운영 경로는 실험 B로 이미 O(N²)를 확증했고, 독립 K축은 운영 시나리오가 아니기 때문이다.
   - 반영: 보고서 1편에 7.6절을 신설하고 요약과 결론을 갱신했다. 중간 K 잔차(약 20%, N에 의존)는 추가 검증 대상으로 문서에 명시만 했다.
3. ~~버전 2(우선순위 큐) 측정~~ 2026-07-16 완료. 보고서 2편 작성 완료.
   - (2026-07-15 결정) 버전 1과 2의 배정 동일성 검증은 하지 않기로 했다. 여섯 버전 모두 "정원이 남은 팀 중 합이 최소, 동점이면 낮은 인덱스" 규칙이라 배정 결과가 같음이 구조로 보장되고, 근거는 엔진 상단 주석에 있다.
4. ~~버전 4(레드블랙 트리) 측정~~ 2026-07-21 완료.
   - 기대("힙이 상수에서 빠르다")를 실측이 부정했다. 교차점은 K가 약 30인 지점이고, 운영 구간에서는 TreeMap이 우세하다.
5. **fork-join `improve()` 병렬화 (P0-1 ③).** 사용자 결정(2026-07-21)으로 6번보다 먼저 착수했다.
   - 진행 상황은 5.3절에 세션별로 정리했다.
   - 세션 프롬프트 파일은 전부 지웠다. 진행 상황은 0절과 5.3절로 확인한다.
   - 현재 위치는 원인을 밝히고 문서로 옮긴 단계다. 1.2배의 원인은 스트림이 평가마다 만드는 객체이고, 9.52GB가 두 편차 메서드의 스트림 생성 세 문장에서 나온다는 것까지 확인했다. 1편과 2편을 합쳐 다시 쓴 것까지 끝났고 성능 분석은 세 편이다. 다음은 최종 보고서다.
   - 교환 반복이라 반복 사이에 의존성이 있다. 그래서 병렬화 전후의 결과 동일성 검증이 필수다. 실패하더라도 원인 분석은 포트폴리오에 쓸 수 있는 내용으로 인정한다.
   - 참고: 로드맵 P0-1의 순서는 1) 프루닝, 2) 증분 재계산, 3) 병렬화인데, 1번과 2번을 하지 않은 채 3번을 먼저 다루는 것이다. 사용자 결정이다. 진행 중에 2번(증분 재계산)과 결합할지는 세션에서 논의한다.
6. **비교 문서 작성(버전 1, 2, 4)과 엔진 최종 버전 결정.** 3편의 "(작성 예정)" 두 곳을 채우는 것을 포함한다.
   - 세션 프롬프트: `docs/병목개선/세션프롬프트_seedGreedy_비교문서.md` (삭제됨)
   - 데이터는 완비했다. 버전 2와 4는 같은 날(7/21) 백투백 쌍이고, 버전 1은 7/14 데이터다. 격차가 150배에서 190배라 날짜 차이가 결론에 영향을 주지 않는다는 점을 명시하는 방식으로 쓴다.
   - 3편의 7.4절("실측 배율이 로그 기대치보다 낮은 이유")과 7.5절의 "관찰" 블록은 사용자 지시로 비워 두었다. 사용자가 직접 쓸 예정이고 Claude는 논의를 지원한다.
   - 엔진 최종 활성 버전(버전 2 유지 대 버전 4 전환)을 비교 문서 결론과 함께 사용자와 결정한다.

---

## 7. 하지 않기로 한 것

- 자료구조 6종을 전부 측정하지 않는다. 측정 대상은 대표 3종(선형 스캔, 우선순위 큐, 레드블랙 트리)이다.
  - "+α"는 레드블랙 트리로 확정했다(2026-07-16).
  - 나머지 3종(정렬된 배열, 정렬된 연결 리스트, AVL)은 같은 계열이므로 대표 측정으로 갈음한다.
  - 2026-07-21에 이 3종을 코드에서도 삭제했다. 사용자 결정이다. AVL 추가 측정도 하지 않기로 확정했다. 직접 구현과 표준 라이브러리를 비교하는 모양이 되어 깨끗한 결론이 나오지 않고, 계열 대표는 이미 측정했기 때문이다.
  - 계열 분류: O(N·K)는 {선형 스캔, 정렬된 배열, 정렬된 연결 리스트}이고, O(N log K)는 {우선순위 큐(힙), AVL 트리, 레드블랙 트리}다. 정렬만으로는 pollMin과 insert를 둘 다 O(log K)로 만들 수 없다. 배열은 이동에 O(K), 리스트는 순회에 O(K)가 들어 O(N·K)에 머문다. 힙과 트리만 둘 다 O(log K)라 O(N log K)가 된다.
- 공간 복잡도는 실측하지 않는다. 필요하면 JMH GC 프로파일러의 할당량(B/op) 한 줄로 갈음한다.

---

## 8. 추후 학습 예정

- 부동소수점(IEEE754 `double`) 반올림 오차와 `1e-12` epsilon 비교 원리. `improve()`의 `newDistDev < bestNewDistDev - 1e-12`에서 나왔다. 지금은 맛보기만 했고 나중에 제대로 학습한다.
- JVM JIT 내부 동작(티어드 컴파일 C1과 C2, 컴파일 임계값, 인라이닝 등). JMH 사용에는 불필요하다고 판단해 의도적으로 보류했다. 필요한 수준은 이미 확보했다. 왜곡 4요인을 한 줄씩 정리하면 다음과 같다.
  1. 워밍업은 JIT 안정화를 기다리는 것이다.
  2. return은 죽은 코드 제거를 막는 것이다.
  3. `@State`는 상수 접기를 막는 것이다.
  4. fork는 실행 사이의 오염을 차단하는 것이다.
  - (2026-07-28 자료 추가) 배경 수준은 이 날 논의로 다뤘다. 왜 JIT이 필요한가, hot 판정(호출 횟수와 루프 반복 횟수), 컴파일이 비동기라는 점, On-Stack Replacement, C1과 C2, 티어드 컴파일, 통계 기반 추측 최적화와 역최적화까지다. 아래는 그다음 단계 자료다.
    1. Understanding Java JIT Compilation with JITWatch, Part 1 (Ben Evans, Oracle). 인터프리터, hot 메서드 판정, C1과 C2, 티어드 컴파일, `-XX:+PrintCompilation` 읽는 법을 다룬다. 여기부터 본다. 저자가 『자바 최적화』와 같은 사람이라 책과 용어가 이어진다. Part 2는 JITWatch 사용법이다. https://www.oracle.com/technical-resources/articles/java/architect-evans-pt1.html
    2. JITWatch. 컴파일 로그를 읽어 어느 메서드가 어느 단계로 컴파일됐는지, 무엇이 인라이닝됐는지 보여 주는 도구. 위 1번이 이 도구의 소개 글이다. IntelliJ 플러그인 형태도 있다. https://github.com/AdoptOpenJDK/jitwatch
    3. Introduction to HotSpot JVM C2 JIT Compiler (Emanuel Peter). Oracle의 C2 개발자가 쓴 연재이고 1번보다 깊다. 지금 볼 것은 아니고 C2 내부가 궁금해질 때 연다. https://eme64.github.io/blog/2024/12/24/Intro-to-C2-Part00.html
    4. JVM Anatomy Quarks 중 2개. 위 GC 항목의 연재와 같은 곳이다.
       - #29 Uncommon Traps. 역최적화를 다룬다. https://shipilev.net/jvm/anatomy-quarks/29-uncommon-traps/
       - #27 Compiler Blackholes. 죽은 코드 제거를 막는 방법을 다룬다. 프로브의 `sink` 변수를 쓰는 이유와 직접 연결된다. https://shipilev.net/jvm/anatomy-quarks/27-compiler-blackholes/
    5. 『자바 최적화』 9장에서 10장. 위 자료 매핑 항목과 같다. 1번 글의 저자가 쓴 책이다.
  - (2026-07-28) 읽기 전에 볼 수 있는 관측 2가지
    1. `-XX:+PrintCompilation`으로 프로브를 한 번 돌리면, 측정 5회 구간에도 컴파일이 계속 일어나는지 눈으로 확인된다. 프로브의 JIT 열이 답하려는 질문을 다른 방식으로 확인하는 것이다.
    2. 컴파일 임계값 기본값은 `-XX:+PrintFlagsFinal -version`으로 이 기계에서 직접 읽는다.
  - 지금도 열지 않는 것: 컴파일러 지시문(Compiler Control)으로 메서드별 컴파일을 제어하는 것, C2의 중간 표현과 최적화 단계 구조, 인라이닝 임계값 조정 같은 튜닝 플래그. 측정에서 설명되지 않는 값이 나올 때만 표적으로 연다.
- (2026-07-27 예약) ForkJoinPool 설계 원리(작업 훔치기 스케줄링)
  - 시점: improve() 측정 작업(스레드 수 스윕, `-prof gc`, 문서 마감)이 끝난 뒤.
  - 자료: Doug Lea, "A Java Fork/Join Framework"(2000) 논문과 `ForkJoinPool` Javadoc 클래스 개요.
  - 목적: 실측한 현상(busyCore, 오버헤드, 스틸 카운트)에 설계 의도를 대응시키는 것.
  - 소스 수준의 내부 구현(워커 작업 큐 자료구조 등)은 예약하지 않는다. 포트폴리오와 면접 요구 범위 밖이고, 실무에서 필요해질 때 본다. 같은 날 논의로 결정했다.
  - 이해의 3층 구분: 1) 모델과 약속은 확보 완료, 2) 관측되는 동작 특성은 현재 측정 중, 3) 소스 내부는 보류.
- (2026-07-27 자료 매핑) 사용자가 가진 도서 『자바 최적화』(Optimizing Java)를 측정 단계별 표적 독서로 쓴다. 완독하지 않는다.
  - 2장(JVM 개요): procCPU를 누가 썼는지 나누는 측정과 함께 읽는다.
  - 6장에서 8장(GC): `-prof gc` 세션 때 읽는다.
  - 9장에서 10장(JIT): 위 JIT 보류 항목이 열릴 때 읽는다.
  - 3장(하드웨어)과 4장에서 5장(성능 테스트와 JMH): 이미 실측한 내용의 복습용이다.
  - 각 장을 "방금 잰 숫자의 해설"로 읽는 순서다. 현상을 먼저 보고 원리를 나중에 본다.
- (2026-07-28 예약) 가비지 컬렉션 기초. 세대 구분(Young과 Old)과 G1 수집기
  - 계기: 프로브(`ImproveCpuUtilizationProbe`)의 `GarbageCollectorMXBean`과 GC 열을 읽으려면 세대 구분을 알아야 한다는 것이 2026-07-28 논의에서 드러났다.
  - 시점: `-prof gc` 측정 세션 전후. 위 도서 6장에서 8장과 함께 읽는다.
  - 읽을 자료 4개
    1. Oracle HotSpot GC Tuning Guide(JDK 17) 3장 Garbage Collector Implementation. 약한 세대 가설, Eden과 Survivor 두 개의 구조, minor 수집과 major 수집의 차이. https://docs.oracle.com/en/java/javase/17/gctuning/garbage-collector-implementation.html#GUID-23844E39-7499-400C-A579-032B68E53073
    2. Understanding Generational GC: Young, Old & Promotion (gceasy). 승격 과정을 그림과 함께 정리한 글. 위 1번을 읽은 뒤 확인용. https://blog.gceasy.io/understanding-generational-gc-young-old-promotion/
    3. 자바 메모리 관리, 가비지 컬렉션 (yaboong). 용어를 한국어로 훑는 용도. 작성 시점이 오래되어 G1 이전 수집기 기준 설명이 섞여 있으므로 1번과 대조하며 읽는다. https://yaboong.github.io/java/2018/06/09/java-garbage-collection/
    4. Java Garbage Collection Basics (Oracle 튜토리얼). Eden과 Survivor 사이의 객체 이동과 승격을 단계별 그림으로 보여 준다. Visual VM으로 관찰하는 절도 있다. https://www.oracle.com/webfolder/technetwork/tutorials/obe/java/gc01/index.html
       - 주의: 대상이 Java SE 7이다. 세대 구분과 승격 설명은 지금도 유효하지만 수집기 현황은 틀리다. 그 문서는 G1을 "Java 7에서 쓸 수 있는 CMS 대체용"으로 설명하지만, 이 프로젝트가 쓰는 JDK 17에서는 G1이 기본값이고 CMS는 제거되었다. 수집기 종류와 기본값은 1번을 기준으로 본다.
  - API 문서 2개 (개념 자료와 별개다. 프로브가 쓰는 계측 API를 확인할 때 옆에 둔다)
    1. `GarbageCollectorMXBean` Javadoc(JDK 17). `getName`, `getCollectionCount`, `getCollectionTime` 정의. https://docs.oracle.com/en/java/javase/17/docs/api/java.management/java/lang/management/GarbageCollectorMXBean.html
    2. `ManagementFactory` Javadoc(JDK 17). `getGarbageCollectorMXBeans()` 설명. https://docs.oracle.com/en/java/javase/17/docs/api/java.management/java/lang/management/ManagementFactory.html
    - 2026-07-28에 위 두 문서를 열어 확인한 것 3가지
      1. `getCollectionTime()`은 문서에 approximate(근사값)로 명시되어 있다. 정의되지 않으면 -1을 돌려준다. 프로브의 `totalGcTimeMs()`가 `if (t > 0)`으로 거르는 근거다.
      2. 수집 시간이 아주 짧으면 수집 횟수가 늘어도 같은 시간 값이 나올 수 있다. GC 시간이 0ms로 찍혀도 수집은 여러 번 일어났을 수 있다는 뜻이다. → 프로브에 `getCollectionCount()`를 함께 찍는 것을 검토한다(미결정).
      3. JVM은 실행 중에 GC 빈을 추가하거나 제거할 수 있다. 목록을 한 번 받아 두고 재사용하면 안 된다. 현재 `totalGcTimeMs()`는 호출할 때마다 새로 받고 있어 이 점은 맞게 되어 있다.
  - 목적: 프로브의 GC 열과 `-prof gc`의 호출당 할당량(B/op)을 해석하는 것. `G1 Young Generation` 빈에만 시간이 쌓이는지, `G1 Old Generation`에도 쌓이는지에 따라 해석이 갈린다.
  - 지금은 열지 않는 것: G1 리전 크기 결정과 Remembered Set 같은 내부 자료구조, GC 튜닝 플래그, 다른 수집기(ZGC 등)와의 비교. 측정에서 설명되지 않는 값이 나올 때만 표적으로 연다.
  - 이어서 볼 자료: JVM Anatomy Quarks (Aleksey Shipilëv). https://shipilev.net/jvm/anatomy-quarks/
    - 형식: 글 하나가 JVM 메커니즘에 대한 질문 하나를 잡고, 실제로 벤치마크를 돌려 숫자로 답한다. 개념 설명으로 끝나지 않는다.
    - 저자: JMH를 만든 사람이고 Shenandoah GC 개발자다.
    - 순서: 난이도가 위 1번(Oracle 가이드 3장)보다 한 단계 높다. 3장을 먼저 읽고 본다.
    - 2026-07-28 논의와 직접 겹치는 글 2개
      1. #11 Moving GC and Locality. 살아 있는 객체를 옮기는 방식이 무엇을 얻는지. https://shipilev.net/jvm/anatomy-quarks/11-moving-gc-locality/
      2. #13 Intergenerational Barriers. 세대를 나누면 생기는 추가 비용. 2026-07-28 논의에서는 다루지 않은 내용이다. https://shipilev.net/jvm/anatomy-quarks/13-intergenerational-barriers/
- (2026-07-28 예약) JVM 스레드 구성과 세이프포인트
  - 계기: `jvm잔여 = procCPU − Java 스레드 CPU 합`에서 GC와 JIT 두 항목만 고른 근거가 약하다는 지적이 2026-07-28 논의에서 나왔다. 잔여의 구성을 확정하려면 이 JVM에 어떤 스레드가 있는지를 알아야 한다.
  - 범위: JVM 개론 전체가 아니라 질문 하나로 좁힌다. "실행 중인 이 JVM에 어떤 스레드가 있고, 그중 `ThreadMXBean.getAllThreadIds()`에 안 잡히는 것은 무엇인가."
  - 2026-07-28 논의에서 정리한 것 5가지
    1. JVM 명세는 내부 스레드 구성을 정의하지 않는다. 구현에 맡겨져 있어서 명세를 읽어도 이 질문에 답이 안 나온다. 필요한 것은 HotSpot 구현이다.
    2. 힙과 메서드 영역은 모든 스레드가 공유하고, 스택과 PC 레지스터는 스레드마다 따로다. `ImproveTask`의 `local` 참조 변수는 워커 스택에 있고 실제 리스트 객체는 공유 힙에 있다.
    3. TLAB: 힙은 공유지만 각 스레드가 Eden에서 자기 몫의 구간을 미리 떼어 받아 그 안에서 혼자 할당한다. 그래서 병렬도를 올려도 할당 자체에 경쟁은 생기지 않는다. 대신 Eden이 더 빨리 차서 Young GC가 더 자주 온다.
    4. Java 스레드(main, FJP 워커, Common-Cleaner 등)는 ThreadMXBean에 잡히고, VM 내부 스레드(GC 작업 스레드, C1·C2 컴파일러 스레드, VM Thread, 주기 작업 스레드 등)는 잡히지 않는다. 후자가 jvm잔여다.
    5. 세이프포인트: JVM이 모든 Java 스레드를 안전한 지점에서 멈추는 장치다. GC 멈춤 구간, 역최적화, 스레드 덤프가 이때 일어난다. 모든 스레드가 도달할 때까지 기다리므로 하나가 늦으면 나머지가 대기하고, 대기 중에는 CPU를 쓰지 않아 busyCore를 낮춘다.
  - 읽을 자료 3개
    1. 『자바 최적화』 2장(JVM 개요). 위 자료 매핑 항목과 같다. `jcmd <PID> Thread.print`로 뽑은 스레드 목록을 옆에 두고 읽는다.
    2. Java Virtual Machine Technology Overview (Oracle JVM Guide, JDK 17). HotSpot 구성을 짧게 훑는다. https://docs.oracle.com/en/java/javase/17/vm/java-virtual-machine-technology-overview.html
    3. Safepoints: Meaning, Side Effects and Overheads (Nitsan Wakart). 위 5번을 제대로 다룬다. busyCore가 설명되지 않을 때 연다. https://psy-lob-saw.blogspot.com/2015/12/safepoints.html
  - 목적: jvm잔여의 구성을 추측이 아니라 이 기계에서 뽑은 목록으로 확정하는 것. 절차는 5절의 2026-07-28 학습 세션 항목에 5단계로 적어 두었다.
  - 지금은 열지 않는 것: 클래스 로더의 위임 모델과 바이트코드 검증, 메서드 영역과 상수 풀의 상세 구조, Java 메모리 모델(가시성과 순서 보장), 스택 프레임의 내부 구조.
- (2026-07-30 예약) 스레드 생성 과정과 스케줄러의 CPU 할당
  - 계기: 4.1 측정 논의에서 getThreadCpuTime()의 값이 운영체제 스케줄러의 기록에서 나온다는 것을 다루며, 스레드가 만들어져 코어에 올라가고 내려오는 과정 자체를 공부하고 싶다는 요청이 나왔다.
  - 범위: 스레드 생성이 JVM과 운영체제에서 어떻게 이루어지는지. 스케줄러가 스레드를 코어에 올리고 내리는 과정. 컨텍스트 스위치가 무엇이고 비용이 어디서 드는지.
  - 시점: improve() 측정 작업이 끝난 뒤.
- (2026-08-14 예약) CPU 캐시와 메모리 접근 비용, 그리고 반복문 한 바퀴에 실제로 드는 시간
  - 계기: 증분 재계산으로 바꾼 뒤 평가 한 번에 걸린 시간이 줄어든 크기가 개수로 예상한 것보다 컸다. 2026-08-04 for문 회차의 병렬도 6 분할 36에서 합 편차 계산이 1회당 1,387ns이고 분포 편차 계산이 1회당 3,200ns였는데, 2026-08-14 증분 재계산 회차의 같은 설정에서 49ns와 82ns였다. 훑는 대상이 팀 20개에서 바뀐 팀 2개로 줄었으니 개수로는 10배인데 실측은 28.3배와 39.0배다. 남는 몫을 설명하려면 배열을 처음부터 끝까지 읽을 때와 몇 개만 읽을 때 메모리 접근이 어떻게 달라지는지, 반복문 한 바퀴 자체에 무엇이 드는지를 알아야 하는데, 그 지식이 없어 원인을 더 확인하지 못했다.
  - 범위: 질문 두 개로 좁힌다. 첫째, 배열 200개를 차례로 읽는 것과 그중 2개만 읽는 것이 왜 100배가 아닌 다른 배수가 되는가. 둘째, 반복문 한 바퀴에 조건 검사와 증가와 배열 경계 검사로 얼마가 드는가.
  - 시점: improve() 측정을 마치고 문서를 낸 뒤. 이 절의 다른 예약보다 앞에 읽는다. 이번 회차에서 실제로 막힌 자리가 여기다.
  - 읽을 자료 3개
    1. 『자바 최적화』 3장(하드웨어와 운영체제). 캐시 계층과 메모리 접근 비용을 다룬다. 위 자료 매핑 항목에서 복습용으로 두었던 장인데 이번 회차에서 표적 독서 대상이 되었다.
    2. Gallery of Processor Cache Effects (Igor Ostrovsky). 배열을 읽는 간격을 바꿔 가며 잰 예제 여덟 개로, 캐시 줄 크기와 캐시 계층이 실행 시간에 어떻게 나타나는지 보인다. 짧고 값이 함께 있어 여기부터 본다. http://igoro.com/archive/gallery-of-processor-cache-effects/
    3. What Every Programmer Should Know About Memory (Ulrich Drepper). 위 2번이 요약해 보이는 내용의 원전이다. 분량이 크므로 상용 하드웨어 구조와 CPU 캐시를 다루는 앞부분만 본다. 연재 1편은 https://lwn.net/Articles/250967/ 이고, 한 권으로 묶은 PDF는 https://lwn.net/Articles/259710/ 에 있다.
  - 이미 이 문서에 있는 것: JVM Anatomy Quarks #11 Moving GC and Locality가 같은 주제를 가비지 컬렉션 쪽에서 다룬다. 위 가비지 컬렉션 항목에 적어 두었고 이 항목과 함께 읽는다.
  - 목적: 측정에서 나온 배수를 원리로 설명할 수 있게 하는 것. 지금은 값만 있고 왜 그 값인지를 말하지 못한다.
  - 지금은 열지 않는 것: 캐시 일관성 규약, 메모리 배리어와 재정렬, 프리페처 동작 상세, SIMD와 벡터화. 측정에서 설명되지 않는 값이 나올 때만 표적으로 연다.
- (2026-08-14 예약) 제네릭의 타입 소거(type erasure)와 다리(bridge) 메서드
  - 계기: 1회차 JFR 기록의 스택 트레이스를 JMC로 읽다가 `Object compute():1015` 프레임이 나왔다. 컴파일러가 제네릭을 컴파일 후에 지우고, 부모 `RecursiveTask`의 서명과 잇기 위해 연결용 메서드를 자동으로 만들어 넣는다는 설명을 들었으나, 컴파일러와 제네릭 구현을 모르는 상태에서는 이해가 닿지 않아 별도 학습으로 미뤘다.
  - 측정 판독에 지금 필요한 것은 한 줄뿐이다. `Object compute():1015`는 컴파일러가 만든 연결용 메서드의 프레임이고, 자기 소스 줄이 없어 줄 번호가 클래스 선언 행(1015)을 가리키며, fork로 넘어간 작업이 실행될 때만 스택에 나타난다. 이 프레임의 무게는 진짜 compute() 쪽과 합쳐 읽으면 된다.
  - 범위: 질문 셋으로 좁힌다. 첫째, 컴파일러가 꺾쇠 안의 타입을 왜 지우는가. 둘째, JVM이 메서드를 반환 타입까지 포함한 서명으로 찾는다는 것이 무슨 뜻인가. 셋째, 다리 메서드는 언제 만들어지고 스택 트레이스에 어떻게 나타나는가.
  - 시점: improve() 측정과 문서 작업이 끝난 뒤.
  - 읽을 자료 2개
    1. Type Erasure (Oracle Java Tutorials, Generics). 소거가 무엇이고 왜 하는지를 다룬다. https://docs.oracle.com/javase/tutorial/java/generics/erasure.html
    2. Effects of Type Erasure and Bridge Methods (Oracle Java Tutorials, Generics). 다리 메서드가 만들어지는 예를 코드로 보인다. https://docs.oracle.com/javase/tutorial/java/generics/bridgeMethods.html
  - 지금은 열지 않는 것: 클래스 파일의 서명 저장 구조, 바이트코드 수준의 메서드 탐색 규칙, 와일드카드와 변성. 측정에서 설명되지 않는 값이 나올 때만 표적으로 연다.
- (2026-08-14 예약) 호출 스택과 스택 트레이스 읽는 법
  - 계기: 1회차 JFR 기록을 JMC로 읽는 세션에서 스택 트레이스를 따라가지 못했다. 화면의 아래 줄이 "다음에 실행된 것"이 아니라 "그 메서드를 부르고 기다리던 호출자"라는 방향, 이미 return한 호출은 트레이스에 없다는 것, 표본 수백 개를 앞부분이 같은 것끼리 겹쳐 나무로 보인다는 것이 전부 처음이었다. 호출 스택과 스택 프레임을 교과서 수준으로만 알고 실제로 본 적이 없다는 것이 드러난 자리다.
  - 판독 자체는 규칙 넷으로 진행했고 이해 없이도 쓸 수 있다. 첫째, 갈래를 위에서 아래로 내려가다 처음 나오는 우리 코드(TeamFormationEngine) 줄의 행 번호가 할당 자리다. 그 위의 JDK 줄들은 그 행이 시킨 일의 내부 과정이다. 둘째, 그 아래 줄들은 실행 경로의 구분이라 판정에 쓰지 않고, 자식 무게의 합이 부모와 같은지만 본다. 셋째, 맨 왼쪽에 붙은 최상위 줄들이 서로 다른 할당 자리이고 각각에 첫째 규칙을 적용한다. 넷째, 백분율은 지금 고른 클래스 안에서의 비중이다.
  - 범위: 질문 셋으로 좁힌다. 첫째, 메서드를 부르고 return할 때 스택 프레임이 쌓이고 걷히는 과정이 실제로 어떻게 되는가. 둘째, 예외와 프로파일러의 스택 트레이스는 그 순간의 호출 스택을 기록한 것이고 왜 안쪽부터 인쇄되는가. 셋째, JMC 같은 도구가 트레이스 여러 개를 병합해 나무로 보이는 방식은 무엇인가.
  - 시점: improve() 측정과 문서 작업이 끝난 뒤. 위의 타입 소거 예약보다 앞에 읽는다. 스택 프레임을 알아야 다리 메서드 프레임 설명이 닿는다.
  - 실습 1개: IntelliJ 디버거로 ImproveEquivalenceCheck 같은 프로그램에 브레이크포인트를 걸고 Frames 창을 본다. 한 줄씩 실행(Step Into)하며 프레임이 쌓이고 걷히는 것을 직접 보는 것이 자료 읽기보다 먼저다.
  - 읽을 자료 1개: Stack Memory and Heap Space in Java (Baeldung). 호출 스택과 힙의 역할 구분, 프레임에 무엇이 담기는지를 다룬다. https://www.baeldung.com/java-stack-heap
  - 지금은 열지 않는 것: JVM 명세의 프레임 구조, 바이트코드 수준의 호출 규약, 네이티브 스택. 측정에서 설명되지 않는 값이 나올 때만 표적으로 연다.
